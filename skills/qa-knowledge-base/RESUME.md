# RESUME · 续接说明(给新会话一眼接手)

> 这是个**文件化**知识库,状态全在磁盘。新会话:进 `qa-knowledge-base/`,读 `SKILL.md` + 本文,即可无缝继续。

## 这是什么
把飞书产品需求文档 ingest 成结构化笔记,供 QA 写用例前检索往期相关功能/历史逻辑/回归点。设计见 `docs/01-mvp.md`(做什么)、`02-architecture.md`(进存取)、`03-tech-stack.md`(选型);规则见 `SKILL.md`;约束见 `CLAUDE.md`。

## 当前进度(截至 2026-09-20)
- **v1 全流程(进/存/取)已建好并验证**。
- **2026-09-20 新增 ingest(3 篇 V1.39.0,批量,小麦/AI Tutor 一条线)**:① `AI Tutor / Mai主对话`——《小麦的消息列表和消息气泡》:对话 UI 从**只支持曝光一轮、STT 不可修正**升级为**通用多轮消息列表**,作为贴纸岛猜词与课程路径对话课的**统一对话底座**;**一个 Kids ID 一个通用列表、多 scene 共用同一条时间线**(参考小红书点点),AI 气泡(文本+重听+字幕对齐+溢出可滚)、用户气泡(波形+点击重听接变声器+STT 转文本+**仅最近一轮可改文本**),输入组件在「点击开启/点击发送」上**加长按录制·松手发送·左滑取消**、**VAD 延到 60s**,`{user.name}` **对接 AI STT 热词**修姓名误识;**列表内对话消耗电量(课程路径接入除外)**,各场景需**先在配置平台注册并配 prompt**(给了贴纸未解锁/已解锁/课程路径三套 prompt)。附**姓名识别异常 7 例分四型**(ASR 误听/角色扮演假名/疑似多人共用一个 kid id/称呼替代真名),原文自注要脱敏 —— **笔记只留分型结论,不转录 kidId 与真实姓名**。② `AI Tutor / 课程路径Mai Node`(**新建二级**)——《小麦接入课程路径》:把小麦封装成**可排进路径的对话课节点**,固定跑 **Onboarding 开场 → Play 小麦游戏模板 → Closing 收尾**(**不要 Word Play、不要 Quiz**),首期接多图故事书;Admin **新建课程类型 `Content type: Mai Node`**,含 **Conversation Guide**(prompt + 可用参数:母语/年龄/学习等级/level·unit/已学单词与日志(看·听·读) **或** 教学大纲+完课状态,**方案一二并列未选**)与 **Lesson Material**(填 scene id + 启用开关):**启用时按多图故事 `read_page/max_page` 映射星级(0 / 0–33% 1★ / 33–66% 2★ / 66–100% 3★,向下取整)**,**不启用时走完一轮完整对话即三星**;**一个入口暂定只绑一个 scene id、不走小麦推荐**,**非目标语言用户一律指定 language 为英语**,**不支持离线、不消耗电量、不显示电量组件**。③ `AI Tutor / 场景内口语对话课`——《小麦多图故事支持接入 NPC 体系》:多图故事角色接入 NPC 对话(= **让 M Town Talk 支持多图**),**bbox 只绑 `npcId`,人设/形象/音色一律读「NPC 资产登记表」统一真源**;**进入页面自动激活 / 点击激活两种方式按 html config 逐页配**;**同一时刻单一对话对象、点其他 NPC 即切换、小麦本身也是 NPC 可召回**,激活 NPC 时小麦待机;**NPC 要感知翻页带来的场景与时间变化做连续对话**;埋点只有两个**建议且待评审**的事件 `npc_talk_start`/`npc_talk_end`;**存量无 NPC 字段的故事按现状运行、向后兼容**。⚠️ **三篇共同的硬缺口:电量口径三套说法打架**(①写『列表内对话消耗电量(课程路径除外)』、②写『不消耗电量不显示组件』但自己的 D1 又把计费列为待决策、③的 D5 写『是否计电量未定』)—— **必须三篇一起定**;另外 ①②③ 都**没有埋点表,统一指向另一篇《Mai - AI Tutor 埋点》**(未入库)。③ 原文自列 D1–D6 且写明「**进入开发前需补齐:D1 两处笔误澄清、激活/切换状态流转图、NPC 上下文规则、电量计费口径、埋点评审**」—— 当前状态**不具备写完整用例的条件**。⚠️ **原料缺口(第二次遇到)**:①②两篇的流程图与组件图示都是**飞书画板块(block_type=43)**,fetch 不抓 —— 已在笔记里如实标注未读取、不做推断。**fetch.py 补画板抓取的优先级可以提上来了**。
- **2026-09-20 新增 ingest(1 篇 V1.39.0)**:`任务体系 / 任务面板与任务列表`——《任务2.0 日常任务和回归任务补充说明》,V1.38《任务 2.0》的补丁篇(正文仅 399 字、0 图 0 表),两件事:① **老用户重装/升级后的路径归属**:**老用户(此版本安装包以前已注册过的游客 id)卸载重装或升级后启动,不再走新人 onboarding**;但**原游客 id 已转正式 id 时设备会新生成一个游客 id,仍要走【选年龄】+【定计划】**(退出登录回欢迎页同理)。② **按生命周期的三条命中规则**:**注册 5 天以上且不属于「14 天以上未活跃」→ 日常任务;14 天以上未活跃的老用户 → 回归任务;注册 <5 天 → 新人任务且没有弹窗、直接进首页路径**;**三类都在 AB 测,对照组一律直接进入首页**。埋点只加一条:**游客 id 重新安装 App 单独上报一个事件**,开发备注「暂时写了 `guest_account_restore`,需要调整再说」—— **事件名未定稿**。⚠️ 待确认 8 项,硬缺口:**「14 天未活跃」口径是否复用同版本《用户标签管理系统》的 `lifecycle_stage` T+1 标签**(不一致会出现同一用户两套判定)、**注册 5 天与 14 天未活跃的优先级只能反推、原文没显式写**、**判定时机(冷启动/每日0点/进首页实时)未写**、**重装后任务进度与回归任务「30天内学3天」计数是否继承**、**新游客 id 注册时间是新的会命中新人任务 → 老用户可能反复吃新人奖励**、**对照组「直接进入首页」是否也跳过选年龄+定计划**。⚠️ **原料缺口(新类型)**:文档「路径说明」下是一个**飞书画板块 `block_type=43`(board,token Des1whdRch4o65blUgRlk7gagah)**,正文反复引用的「上述流程」就画在里面;**fetch 目前只抓 30/18(Sheet/多维表),不抓画板** —— 该流程图未落盘、笔记里**如实标注未读取且不做推断**,要看分支得去飞书原文打开画板。**这是 fetch.py 值得补的一类块**(已记在待办想法里)。
- **2026-09-20 新增 ingest(1 篇 V1.39.0)**:`用户标签系统 / 标签定义与人群圈选`(**新建一级模块 用户标签系统**)——《用户标签管理系统》,把「按用户特征圈人」从各业务里抽出来做成独立系统:后端统一定义与计算标签(**实时** = 值由业务状态直接映射且变更点明确、由**业务方状态变更时主动回调写入**;**T+1** = 跨时间窗聚合或需关联多数据源,**每日 00:00 跑批**),标签值记在用户身上,支持按标签组合批量筛人;**目标人群选择器是对业务侧的唯一引用组件**,推送/任务/活动三系统复用同一份,各自只在配置页新增一个「目标人群」字段。选择器规则:包含区**【全部条件】AND /【任一条件】OR**、**无条件视为全站用户需二次确认**、排除区**组内固定 OR**、**执行顺序恒为『先算包含,再减排除』**、下拉**只列已启用标签**;运算符与控件**由值类型自动决定**(枚举 属于/不属于;数值 大于/小于/等于;布尔 是/否无控件;日期 最近N天内·早于·晚于·介于),右侧实时预估命中人数与占比。标签清单:实时有 `country`(**按打开 App 时的 IP 写入**)/`age_group`/`register_method`/`registered_at`/`account_created_at`/`app_language`/`os_type`/`device_type`/`app_version`/`push_enabled` + 学习状态 `current_level`/`finished_units`/`learned_word_count`/`starter_course_done`/`first_lesson_done`/`last_learn_at`/`giggles_balance` + 运营 `ab_group_<实验名称>`;T+1 有 `active_7d`/`active_days_30d`/`streak_days`(自昨日往前连续、中断即停)/`first_week_days`/**`lifecycle_stage`(未激活·活跃·沉默·流失)**/`is_returned`。**关键设计:业务侧存的是人群定义不是名单快照,标签每天更新人群自动跟着变**;原型自带警示「**投放执行前要二次校验:创建可用 ≠ 执行时可用**(标签可能已下线或跑批失败)」。⚠️ 待确认 13 项,硬缺口:**标签 user 与 kid 两种维度混用、一个 user 多个 kid 时圈出来的到底是谁/推给谁全篇没定义**(最影响正确性)、**T+1 跑批时区未定**(中国+印尼双区会直接改变近7天活跃与连续天数)、**跑批失败与二次校验失败的产品行为空白**、**『禁用后已在用人群不受影响』与原型『执行时标签已下线会失败』两处说法打架**、**生命周期『沉默 14 天内』与『流失 超过 14 天』在第 14 天重叠**、**「业务活跃/活跃/学习行为」三个词混用且无事件口径**、**country 随 IP 跳变但活动按 country 判入口(九月绘本大赛正是此场景)**、**全站人群只做二次确认、无权限与人数保护**、**人群定义能否命名复用未写**;另记:原型列表里有一个**正文清单没有的标签 `family_type`(家庭类型,孩子档案增删时重新判定)**;本篇与 `AB实验平台 / 实验分流机制` 存在同一分组两套真相的风险。
- **2026-09-20 新增 ingest(1 篇 V1.39.0)**:`功能广场 / 入口矩阵与曝光`——《聚合入口部分功能调整》,把首页聚合入口从「功能罗列」改成**按级别拓展内容**(AB key **`default_feature_squarev2`**,对照组=旧版):面板重排三区 —— **「Lv N 拓展 Expansion」**(取用户主路径级别,固定 绘本/Speaking/Phonics,**绘本位展示课程配套推荐的第一本**,ⓘ 进拓展体系介绍文档)、**语感培养**(Giggle Cast→Giggle TV→FlashCard→MiniPlan)、**复习工具**(Arena→Art→Worksheets→Deskmate→Search)。绘本馆同步改版:顶部 **推荐 + A/B/C/D/E 共 6 Tab、默认推荐页**;**主路径推荐页**三位 = **课程配套推荐(按 Level 推 4 本、可刷新换主题、学完换新、不够 4 个展示完成态)→ 推荐绘本系列 → 随便听听(L1–L2)/开口跟读(L3+)**;**自然拼读推荐页** = 后台配的 3 个系列 + 开口跟读;**「我的声音」改名「我的作品」**,搜索/我的关注/我的收藏收进「...」菜单,三页去 spine 引导改静态缺省;**后端按表重刷全部绘本 lexile_score 并打 A–E 难度标签**(取表 I 列 Target lexile_score);后台在 Giggle Creator 新增**推荐管理** tab(按 Level 分卡片配推荐系列 + 栏目绘本,支持添加级别/栏目、按 ID·名称·系列名模糊搜索多选)。另含四件小改:**FlashCard 右上角加 Word 入口 + 学习中心面板移除单词库**、**MiniPlan 改全解锁**、**Giggle Cast / Giggle TV 支持 0.5–2.0X 倍速(每次进入重置 1.0)+ Giggle Cast 加首页播放组件与播放列表(切专辑不自动播)**、**单元卡片加「打印练习册」+ Admin 单元编辑上传关联练习册 PDF**。埋点在内嵌表(60×12):聚合入口 `homepage_feature_menu_open/close` 加 `course_path`、`app_page_view` 带 `section`;绘本馆一整套 `storybook_*` + **`StoryImpression`(可视面积≥50% 触发一次)/`StoryClick`(防连点、下架书不报)** 带 `entry_tab`/`route`/`position`/`story_level`;倍速 `playback_speed_change` 带 `speed`/`source`。⚠️ 待确认 15 项,硬缺口:**课程配套推荐位规则自相矛盾**((1)(2)说按主题推荐+刷新切主题、(4)又说「不跟主题关联、级别下随机」)、**第三位正文写 3 本+查看更多、设计稿画了 4 本且无该入口**、**蓝思值→A–E 的分档边界没给且刷数据无回滚方案**、**自然拼读用户/未开始主路径的新用户在「Lv N 拓展」显示什么没写**、**AB key 覆盖范围不明**(只控面板还是连绘本馆改版/闪卡入口/倍速一起控 → 直接决定用例矩阵)、**打印练习册的产物与链路空白**、**「我的订阅」与「我的关注」是否同一个东西**;另记:本篇的**课程配套推荐依赖《知识体系后台改造方案》的绘本关联知识点**,原文明说「本期不一定来得及做完,做不完产品给一张映射表」。
- **2026-09-20 新增 ingest(1 篇 V1.39.0)**:`知识体系 / 学习资产与标签管理`(**新建一级模块 知识体系**)——《知识体系后台改造方案》,把散在各后台的单词收敛成一套知识库:**让单词成为实体(多业务引用同一知识点 ID,消灭自由文本与冗余文本列)+ 打通反查**(现状:MiniPlan 复习游戏是纯自由文本、闪卡靠 CSV、课程单词是独立表、绘本压根没关联,且**单词无 id、`apple` 与 `Apple` 是两个词**)。「学习资产列表」提升为**知识体系 / 学习资产列表**(每条 = 知识点,含单词/短语/句型,15 位 ID,`+N` 角标 = 非原形变体数),新增**标签管理**(主题/体系/级别,`key 标识不可改`、**有知识点的标签不可删**,数量实时统计不落库);**左侧分组是按字段现算的视图、不是实体树**(词身上只挂 主题/级别/体系 三个普通字段,切换分组只换字段重算,不存两份);**状态三值线性**(待上架 →(完整才可)→ 已上架 →(下架)→ 已下架 →(完整才可)→ 已上架),**完整度 5 项实时算不落库**(主题/级别/图片非『待生成』占位/原形音频已上线/**所有启用变体各自音频已上线**);**变体与音频**:复数·过去式等 9 种形态枚举,**同形(如 sheep)不新建变体只在原形标注**,**标准发音挂在形态上不是挂在知识点上**(dog≠dogs),新增默认草稿;**引用反查 8 个类目**(课程/闪卡/绘本/课程游戏/Quiz/M town 对话课/家园家具/MiniPlan)各有列结构;**通用【关联知识点】选择组件**(引用形态可选原形或变体、已选 chip 蓝色=指定非原形、无变体的词显示「仅原形」)。接入优先级 **P1 主路径课程 + 绘本 / P2 游戏大厅·小Mai·每日复习 / P3 闪卡·MiniPlan**;课程单元编辑的【从素材库选择】改成【选择知识点】,**历史学习数据不计算掌握度、从更换后开始**。⚠️ 待确认 15 项,硬缺口:**§待拍板的正主 —— 编辑历史数据是否强制补全**(现行规则下改个错别字也要先补发音,影响 68 条/其中 49 条已上架)、**正文『变体要去掉音标和描述』但设计稿里都还在**、**标签管理正文 3 个 tab 实拍只有 2 个且阅读级别 L0–L4 vs A–C 打架**、**保存不校验完整度但上架要求完整 → 已上架被改成不完整怎么办没定义**(列表页却真有『已上架 + 缺 N + 红 !』这个态)、**删除变体『回落到原形』的具体行为与已有掌握度怎么算**、**大小写重复数据(apple/Apple)的合并规则**、**批量导入写『待补充』但按钮已在**、**全篇无埋点与权限/操作日志**;另记:**C 端三处单独维护单词、后台无对应功能 —— 游戏大厅小游戏出题规则、speaking 取词、单元结算卡片单词展示**,原文只写『需要测试验证』,是回归边界最模糊的一块;截图里还出现正文没提的**「覆盖度分析(新)」**入口。
- **2026-09-20 新增 ingest(1 篇 V1.39.0)**:`挑战课与Quiz / Quiz交互`——《单元复习课 · 新增题型》,单元复习课此前**只有【音频-图片配对】一种题型**,为 **L6「认识单词 + 短句填空」**新增 4 种:音频-文本、音频+文本-文本、**音频+图片-文本**、**填空题**(线索 = 带空格句子文本 + 读完整句的音频,**两个独立字段**)。三块改动:① **线索组件**支持**长文本自适应(宽度随内容收缩)** 与 **图片+音频**;② **单选题文本选项**(后台配【文字模式】时端上渲文本卡,**三卡等宽、高度自适应、字号按最长选项统一降档且三项必须一致**);③ **填空题**答题态显示 `I am ___ .`,**答对后把选中的词填进空格(绿字+绿下划线)、展示完整句并重播线索音频**;Admin 新增 `Fill-in-the-Blank` 手动配置(Knowledge Point 从词库选、题干信息带下划线占位、线索音频从素材库/学习资源列表引用、选项从词库选 + 指定正确答案),并给线索配置加**「是否展示题干信息」开关**与**单独引用素材库音频**。🔗 **闭环了 V1.37 的遗留缺口**:《单元测验线索提示类型渲染》记录「Clue 的 Image / Image+Audio 后台能配但端上无图片区、兜底不展示」,本期端上实现图片线索 —— **那批按兜底逻辑配出来的存量题会随本期上线改变表现,必须回归**。⚠️ 待确认 12 项,硬缺口:**空格占位符正文写「一个下划线」、设计稿写「三个下划线」**、**填空题选项正文说支持图片、设计稿说固定文字不可选图片**、**Knowledge Point 正文单选 / 设计稿可多选**、**答错交互完全没写**(只写了答对)、**「是否展示题干信息」新字段的默认值与存量影响全空**、**多空 / 超长目标词渲染未定义**、**全篇无埋点章节(答对率无法按题型拆)**。
- **2026-09-20 新增 ingest(1 篇 V1.39.0)**:`运营活动 / 绘本配音大赛`(**新建一级模块 运营活动**)——《P1 App - 绘本跟读配音(九月活动)》,一次限时 UGC 活动 **Storytelling Contest**:首页顶栏「学习中心」胶囊右侧新增 **Voice Contest 入口(带 New 角标,只对中国/印尼展示,用新增的首页运营活动广告位配 Banner,活动结束无需发版可换/下线)**;选 **3 本固定难度绘本之一**(简单/中等/困难,给了 story id),**完全复用现有故事书逐句跟读页**(示范音 → 跟读 → 自动下一句,左右箭头任意句重录不限次),**最后一句录完自动合成整篇并提交**(提交内容 = 音频 + 绘本ID + 用户ID + 孩子ID + 区域标签);提交成功**立刻进公开【作品广场】(不分区域)** 且**即时按账号发一套活动专属套装**(可在装扮里穿戴);广场 Tab = Top Picks(点赞降序,**前 3 位可运营指定**)/ New Stories / My Story,**【换一批】抽 8 个 = 高赞前20%随机3 + 最低曝光30%随机3 + 近24h随机2**;站内点赞每人每作品 1 次**可取消、不能赞自己**;**站外链路是核心设计「点赞在下载之前」**:分享卡 → H5 播放页(免登录免下载)→ 播放 → **按设备 id 限一次、不可取消的点赞** → **点赞成功「之后」才出下载引导** → 跳 App Store / Google Play(中国走对应应用市场),漏斗六层各有事件名(ShareSent/H5Opened/WorkPlayed/LikeSuccess/DownloadCTAShown/DownloadClicked)且**每层都要带分享者 user_id + 作品 id 才能归因**;活动**只有一个结束时间**:到点同时停止提交、停止点赞、公布榜单(中国榜/印尼榜分别排名、平票并列、仅获奖者可见收货信息入口),中国加微信 / 印尼加 WhatsApp 联系,运营两周内发货,公示两周后下架。⚠️ 待确认 15 项,硬缺口:**获奖名额三套口径并存(正文与规则文案 Top 10 / 泳道图各区 Top 3 / 设计稿统一 Top 5)**、**「一人一个作品」与「每人每本一个」自相矛盾**、**入口条件一处写用户区域一处写 IP**、**站内可取消 vs 站外不可取消且未说同一人两端是否重复计数**、**防刷只有『实时风控』四个字、无判定与处置**、**内容下架后点赞/排名/套装是否回滚全空**、**原文自认「官网绘本播放会卡住,需先解决」(站外漏斗第 3 层阻塞)**、**广场缺需求要求的难度筛选入口**、**活动时间仍是【上线日】【公布日】占位符**;另记:**本篇没有埋点表,「埋点事件」一节是指向另一篇飞书文档《故事书大赛埋点》(wiki/YXUzwrU8yiF08zk8rFOlT9VGg1g)的引用**,本篇只有 4 项观测口径 + 完整漏斗;口径里仍写 `app_share_click`,与《泡泡单词》0917 改造后的 `arena_share_open` 需对齐。
- **2026-09-20 新增 ingest(1 篇 V1.39.0)**:`IP打电话 / 电话机与来电互动`——《打电话二期优化功能》,把一期的**固定脚本 + 三语预生成音频**整体改成 **AI 生成的场景库**:内容单元「完整台词」→**一句话事件条目**、音频→**实时 TTS 零预生成**、多语言→**AI 本地化**、首次能**实时接住孩子的回复**;四层架构 **L0 常量(人设/世界观/儿童安全红线,语言策略标『待定 D1』)· L1 触发(触发器/主被动分层/频控/优先级)· L2 场景库(事件池 + goal 模板,可能每周上新)· L3 上下文(时间/记忆/学习/行为)**;**链路:触发判定与指令渲染在服务端,agent 对话在客户端**(客户端只拿到已渲染无占位符的自然语言指令 + runtime 配置),通话结束写结构化摘要回记忆层。**被动来电 1–3 轮、话题由事件池预置、仅 Max**;**主动来电前期不限轮次不限话题、全 IP 可选**,两者**共用同一记忆层**。**频控降频(本期核心)**:D1/D2 每日 1 通 → **D3 起改事件触发(成就/活动/生日/节日)+ 距上通 ≥N 天(N 初始 3)兜底**;**「取消订阅」状态与语音信箱整体取消**(线上已退订用户恢复来电但不补前两通),**主动拨打与系统来电不再互斥**;场景优先级 **成就 > 节假日 > 常规轮换池**。场景三件套给了 `school_start` / `ask_word_help` / `open_freetalk` 三个完整样例(触发条件、冷却 5–7 天、age_band、事件池、含占位符的 goal_prompt 与答对/答错/沉默三分支、runtime 的 max_turns 与 sfx 白名单、fallback_audio)。首批场景 7 个冷启动话题 + 4 个运营场景(`weekend_plan` P3 周六 / `comeback_greeting` P2 距上次活跃≥5 天 / `leaderboard` P0 段位变化 / `streak_congrats` P1 连续 7·14 天)+ 主动来电 `open_freetalk`;**埋点是在一期事件上加参数**:`calls_incoming_show`/`calls_answer_click` 加 **`type`(运营场景 + first_day/second_day)**、`calls_decline_click`/`calls_finish` 加 **`source=dial/answer`**、`calls_finish` 再加 **`AI_turn` 对话轮次**。⚠️ 待确认 14 项,硬缺口:**L0 语言策略仍是『待定 D1』**(多语言用例写不了)、**退订/语音信箱下线的数据迁移未定且埋点里还挂着退订口径**、**轮次上限三套值(1–3 / max_turns 3 / 5)**、**AI 失败兜底只在一个样例里出现过 fallback_audio**、**儿童安全红线只有约束没有拦截与判定机制**、**对话留档却是 P2(出事无法复现)**、**埋点 `secord_day` 拼写错误**;另记:文档正文**明文贴了一条真实 admin JWT**(配合 `/admin/kid-phone/scene/pin` 强制指定场景的调试接口),笔记只记接口与 sceneId 枚举、**不转录 token**,已在待确认里提示删除或轮换。
- **2026-09-20 新增 ingest(1 篇 V1.39.0)**:`奖励中心 / Giggle World与社交广场`(**新建二级**)——《P1 功能广场入口》,把**家园 / 游戏大厅 / 广场**收进新地图 **【Giggle World】**(AB:新用户 `reward_function_plaza`、老用户 `reward_funciton_plazaold`(**原文拼写 funciton,疑似笔误**),另加后端开关;新用户第一个单元后走购买弹窗引导、老用户完成当天首个单元弹「A new world is open」)。核心是**社交广场 Square**:**进入 Giggle World 及子功能(家园/游戏大厅/广场)且非游戏中的用户都会被渲染进广场**(进游戏或 PK 即移除),服务端按房分流(**预设 N=10 个坐标点、进房后不变**,Admin 单房上限默认 **20、范围 2–50**),点好友 Avatar 出名片(Lv / 国旗 / 排行榜排名与点赞同源)可**访问家园**或**发起 PK(本期限免不扣 Giggles、结算页即时到账)**;新增 **表情商店 Moves shop** 作为 Giggles 新出口 —— **按动作独立计次的次数包**(机械舞 x10),广场/家园单击即用、**用一次扣一次并写使用流水**、次数 0 时原地弹购买浮层;配套 Admin 两块:**运营/功能广场配置**(背景图 + BGM + 默认动画库 + 基础参数,**常驻默认 + 定时生效、同时段取优先级大者、无命中回落常驻**,节假日到点自动切换;广场 BGM 总开关但**端内音乐开关优先级更高**)与 **奖励中心/表情动作**(编码/获取方式(购买·免费·任务奖励)/单包次数/价格/使用场景/排序/上线·下架·草稿);另有**昵称对他人匿名化**(2–3 字符 `Al→A*`;≥4 字符前 1 + 后 2 `Sunny→S**ny`)、**家长屏幕时间开关关闭后地图入口置灰**、**家园访问要从 V1.35 的假数据换成真实访问记录**。PK 业务规则齐全(**邀请有效期 7s、同对象冷却 60s、当日被邀上限 5 次、同时进行中 1 个、先到先得、学习中不打扰**)+ 5 条异常分支。⚠️ 待确认 14 项,硬缺口:**老用户 AB key 疑似拼错**、**「谁出现在广场」正文与内嵌表口径不一**、**坐标点 10 < 单房上限 20(可配 50)且「进房不变」与图注「每次进出随机分配」冲突**、**表情动作清单 PRD 与 Admin 截图两套价格/免费项/场景全对不上**、**动作槽位 6 个送 4 个 vs 免费只有 2 个**、**邀请有效期 7s 与 UI 两处 5s 倒计时冲突**、**PK 限免与游戏大厅 20 Giggles/次的关系未定**、**全篇「四、数据埋点」只有标题、无任何埋点方案**(而业务目标全靠数据验证)。
- **2026-09-20 新增 ingest(1 篇 V1.39.0)**:`竞技场 / 游戏大厅与小游戏`——《App - 单人游戏 - 泡泡单词》,游戏大厅新增单人小游戏 **泡泡单词**:以**出题组**为单位(一组泡泡升起 → 语音读一个词 → 点破目标),**同组形式不混排**,**点错 / 漏掉各扣 1 ❤️,3 颗掉光结束(无时间限制的无尽模式)**;三档难度差异在同屏泡泡数 3/5/7、内容(图片 / 单词 / **cvc 尾音单词由 AI 出题**)、穿屏速度 9–12s / 6–8s / 4–5s、难度系数 x1 / x1.5 / x2;**泡泡潮**(进度条 10 格,答对 +1、连击≥5 时 +2)跑满触发:全场提速 **x1.3** + 连出 3 组彩色泡泡(+50×系数),**3 组全对补 1 ❤️(封顶 3)**;单次得分 10×系数,COMBO 从连对第 2 次起闪;排行榜取**历史单局最高分**、全国/全球两 tab、未进榜显示 **99+**;扣费与分享沿用游戏大厅(20 Giggles/次)。**埋点是本篇最大变更(以内嵌 Sheet「0917 版本」为准,正文埋点表已作废)**:参数由英文枚举改**中文枚举 `game_name=「泡泡单词」`**、新增 `scene`(游戏大厅/主路径)与 `max_combo`;**三个事件改为 9 款游戏共用的公共埋点** —— `$pageview`→`arena_game_intro_view`(原 `$title` 恒为全局标题不可按游戏区分)、`app_share_click`→`arena_share_open`(原生面板拉起成功后)、`app_share_completed`→`arena_share_channel_click`(`share_channel` 原文透传不收敛);全部标「已完成」并有 CDP 本地实测 + HarmonyOS 真机抓包结论。⚠️ 待确认 12 项,硬缺口:**英文 `bubble_word` 与中文枚举两套口径并存**、**`scene` 只有两个值但入口有三个(分享外链/免登录试玩归属未定)**、**泡泡潮中途答错后的分支与加速叠加规则全空**、**彩色题组是否计入答对率分母**、**`app_submit_question` 缺 difficulty/scene 无法按难度拆**、**miss 是否断连击**、**无尽模式「完成」定义与中途退出是否上报 `app_game_complete`**、**扣费时机与退费仍沿用 V1.37 的空白**。
- **2026-09-08 新增 ingest(2 篇 V1.38.0)**:① `反馈与社区 / WA推送体系`——《WA链接展示形式优化》:WhatsApp 推送里的链接从**纯长链接**改成**带插图的卡片**(每功能一张 **200×200 插图** + 文案 + 跳转按钮),正文给出 **14 个功能入口的 QA/正式 Scheme 与逐条实测落点**(QA=`qakjukl.net/open-app?giggleScheme=`、正式=`giggleacademy.com/open-app?giggleScheme=`)。**关键实测结论**:走 `unity/customsendmessage?objName=TopBar_New(Clone)&methonName=OnToolboxItemAction` 的 **6 个入口(闪卡正式/专题课/自然拼读/绘画/歌曲/口语)全部只能落主路径**;**练习册 QA 白屏**;**闪卡 QA 走 `h5/flashcard` 能直达、正式走 customsendmessage 只到主路径**;`unity/game_home`、`unity/storybook_home`、`flutter/giggleCastList`、`h5/ai-tutor#/deskmate`、`h5/honor-wall#/leaderboard` 均正常;**家长中心学习报告 QA 与正式 H5 路径不同(learned-lessons vs weekly-learning-report,V1.35 就记录过、至今未解决)**。所有外链先落 `open-app` **中间页**(落地页 + 30s 视频 + 「在 APP 上玩」按钮)。② `闪卡 / 主题入口与重复学习`(**新建一级模块 闪卡**)——《App - 闪卡「主题可重复学习」》:修客诉「主题学完后点击没反应」,**主题卡在所有状态下都可点击**,已学完主题**从第 1 张卡完整重学全部单词**,学习中则续做未学习单词;**复习模块(每日复习)本次不改动**。⚠️ 硬缺口已入 pending:**customsendmessage 型入口只能落主路径是 V1.35 老问题复发、哪些本期必修未拍板**、**「插图/文案/修正链接」三列全空**、**正式链接都没带要求的 channel 参数**、**闪卡篇「全部单词」与「推荐一组」自相矛盾**、**重学是否写记忆状态未定(直接影响每日复习的遗忘曲线)**。两篇待确认共 22 项。
- **2026-09-08 新增 ingest(1 篇 V1.38.0)**:`功能广场 / 鹦鹉学舌变声机`(**新建二级**)——《鹦鹉学舌》,功能广场新增独立玩法模块,**核心是「零评分、零失败」的开口场景**(背景:现有语音交互全是评分制,读了被判 0 分 = 负反馈)。① **L0 自由学舌**:全自动收音无按钮,尾静音/录音上限触发判定,VAD 有声则**变声复读**(边缘光波紫→绿、鹦鹉喙随振幅开合),无声则鹦鹉「夸张凑近聆听态」;角色反应分级(正常说话/音量大/长句>4s/词卡连读对 各有不同动画)。② **变声药水 9 种**:基础 4 默认 + **按累计开口次数解锁 4**(果冻 3 次 / 电台 6 次 / 反转 9 次 / 回声 12 次)+ 神秘药水(随机抽,**含未解锁**);逻辑写本地、弱网可用。③ **L1 词卡挑战**:按【主课程】Level-Unit 分组(**本期不兼容 Phonics**),**五种题型随机**(普通读词卡/猜谜卡/悄悄话卡/听音卡/剪影卡,首张固定普通卡)×**三种卡型**(金卡 20% 2 星 / 闪电卡 20% / **大魔王卡每包最后一张固定、护盾需命中 2 次**);**无论读对读错鹦鹉都先变声复述**,再用原声做识别;**三段保底梯**(示范 → 0.75 倍速慢读 → 系统带读点亮给 1 颗空心星,**空心星最多 1 颗、图鉴中不算读对**);未命中**只清零连击、不做任何负面表现**。④ **星格与熟练**:每词 5 格,实心星=真实命中,**实心满 5 = 熟练**,**空心可被后续真读对转成实心**;两种轮次(全部学习轮 / **刷星学习轮**只出未满星词)。⑤ **单词图鉴**四态(未接触黑剪影??? / 接触过半透明 / 实心星点亮 / 熟练金框),非未接触可点听标准读音。⑥ 新手引导 5 节点(Max 入场示范 / 首次成功庆祝 / **第 2 次开口后引导药水** / **第 4 次开口后引导词卡** / 首次点亮卡片后引导图鉴),右下角常驻【Max】求助入口(8 条示范句池,演示期间停收音但鹦鹉保持聆听态、复述沿用当前药水音色)。⑦ 进度**按 kid 维度**服务端存储 + 本地缓存,弱网先写本地、恢复同步,**冲突取较大值或并集**。埋点 12 个 `parrot_*` 事件 + `app_page_view` 新增 `parrot_talk`。⚠️ 硬缺口已入 pending:**大魔王卡第 2 次命中后的规则是一段口述残留坏句**、**需求范围承诺的「判定降级策略」整段缺失**、**模块命名三套并存**、**金卡与闪电卡同为 20% 且未定互斥关系**。本篇待确认 16 项。
- **2026-09-08 新增 ingest(5 篇 V1.38.0,批量)**:① `单词回顾 / 每日复习`(**新建二级**)——《App - 新增任务【每日复习】》:任务中心新增 `Daily Review` 任务卡(**已学词 ≥4 且非使用首日**才出现、本地时间 0 点刷新、当天完成收进「已完成任务」),每天从 learned 词里出 **4–6 题**(前 4 图片音频三选一 + 后 2 跟读),核心是每词一条**记忆状态**(`mastery_level` **L0–L5**,答对 +1、**答错 -2**;遗忘曲线 **1/2/4/7/15/30 天**;`review_count` 还要在**课程路径的记忆卡片配对/单元测验/升级测验**三处累加),结算给正确率环 + 星级(满分 3★ / >30% 2★ / ≤30% 1★)。② `排行榜 / Top Learners学习之星`——《支持段位》:周榜之上加 **7 级段位**,**前 10 名升一级、11–30 不变、只升不降、每周最多升一级、最快 6 周封顶、不发额外奖励**;排行榜页顶部加段位进度条(已达到/当前/未达到三态)、榜单行加段位标签、**第 10 与 11 名之间加晋级分割线**、个人形象卡加段位、晋升后出庆祝页;埋点新增 `app_tier_celebration_show/click` 并给 `app_leaderboard_show`/`settlement_show` 等加 `user_tier`/`tier_changed`/`current_tier`。③ `语音识别 / 跟读组件改版`——《录音组件优化-取消》:Max **放大 + 从左下角内移到演示区旁**,7 状态动效重做(**新增「邀请开口」态**、通过态幅度最大、**未通过取消全屏红光改鼓励招手**、超时探头凑近),四周光波加厚且随音量呼吸;录音未通过给**英文语音提示**(有声 4 级 `Try again!`/`Listen.`/`With me!`(齐读放宽阈值)/`Nice try!`(保底通过);无声 3 级 `Your turn!`/`I'm listening.`/`Say it!`),**判定后 300ms 起播、提示与目标内容间隔 400ms、播完立即开麦**,**同一目标累计 4 次未通过保底通过**,两条线由 VAD 区分独立计数。④ `课程内容 / Speaking跟读课`——《Hello 课跟读统一与开口引导》:Hello 课接入**标准跟读组件**(Max 左下头像 + 紫色波纹 + 自动收音),跟读前插 **≤12s 开口引导小窗**(4 镜 AIGC 分镜,画面冻结压暗、暂停 Unity,播完 0.5s 后弹 Max 头像;Colors1 无开口导致识别失败则重播 5s 精简版),并做**课程包内 A/B**(A 多朋友 3 次开口 vs B 原版 1 次,**kid ID 之和奇偶分桶、首次进课锁定、两版共用同一 lesson_id/course_id 只靠 `hello_variant` 区分** —— 原文强调上次 pathway A/B 因拆 course_id 导致口径脏);背景数据:**首轮开口率仅 54.6%,第二轮回升 70%**。⑤ `学习路径 / 单元内连续学习与单元结算`——《单元结算新增跟读句式样式》:结算页**句式与单词混排(句式在前)**、四项指标(学习句式/学习单词/开口次数/互动次数)**为 0 则隐藏**、新增**关键词 key word**(取得分最高的一次音频 + 发音次数),音频来源含课程内跟读与跟读挑战课的单词与短句(**短句卡片样式有区分**),**语音识别音频要上传并按 level/单元id/用户/单词 四维存最高分**。⚠️ 硬缺口已入 pending:**每日复习的「选词优先级/每日数量」整段缺失**、**段位命名三套并存(TopStar LvN vs Cadet… vs 训练生…)**、**跟读组件篇标题带「-取消」且版本待定**、**Hello 课「kid ID 之和」分桶定义不明**、**单元结算篇关键词无配置入口**。五篇待确认共 65 项。
- ⚠️ **relate.py 新发现一类误连(2026-09-08,待修)**:**Figma 文件 ID `ILqThciQTufJKCFPmIuDVH` 被当成共享功能标识符** —— 同一版本所有需求的 UI 链接都指向同一个 Figma 文件,导致 V1.38.0 的《每日复习》《Top Learners 段位》《IP 打电话》《邀请同桌 1.3》《任务 2.0》五篇**彼此全连**,与历史上 `level_number`/`image_count`/`block_type` 同类(黑名单第四次失守)。建议按**规则**而非黑名单处理:过滤长度 ≥16 且大小写混合无分隔符的 hash 型 token。修复后重跑 `relate.py --write` 即可修正 related。
- **2026-09-08 新增 ingest(1 篇 V1.38.0)**:`任务体系 / 任务面板与任务列表`——《任务 2.0》,在 1.37 新人任务之上扩**常规任务**与**回归任务**,并把任务全量搬进后台 Campaign 配置(沿用 1.37 的任务 AB key)。① **新人任务**加时限(**注册前 5 天命中、day6 结束转常规**,邀请任务不计),任务 1 固定「完成第 1 单元」,**任务 2、3 按 Onboarding 家长/孩子动机动态生成**(唤醒小麦/跟读绘本/去 Giggle TV/游戏大厅/自然拼读,兜底=唤醒小麦+跟读绘本);Onboarding 选 With a parent 走计划型结果页(带 Exclusive Task 三条)、选 child 走卡片式结果页。② **常规任务**:必做「每天完成 1 节课」**完成才给日历打勾 + 点亮火苗**,Bonus Task(含 Daily Review)由后台配、**不影响打卡**;进入常规任务有四种时机(注册 day6 / 完成新人任务领奖后 / 回归任务完成后 / 老用户被命中),统一走「Now new tasks every day」云弹窗。③ **回归任务**:**14 天未活跃**(定义=未用主路径课程/小麦/聚合入口/家园,14 天可配)启动即命中,**30 天窗口期内完成 3 天学习**、完成 1 天领 1 天奖励(服装/Giggles/家具),**关联到 kid**(切 kid 弹回归、新建 kid 触发新人),同一用户只命中一次同一回归任务;面板含 `Try something new` 跳新功能。④ **后台**新增 任务体系 → Campaign 管理 / 日常任务池 / DeepLink库:Campaign 可配 生效失效时间、**触发条件(注册 <M 天 / 上次活跃 >N 天,支持「或」)**、**结束条件(完成全部任务 / 注册满 N 天 / 完成 N 项 / 累计达成天数,满足任一即结束)**、整体配图;任务可配 类型(体验功能/完成课程/完成复习/打开通知权限/邀请好友/查看功能)、**频率(完成 1 次 / 每日 1 次 / 累计完成 N 天)**、奖励、DeepLink。⑤ 内嵌表给了 **13 条任务 Code 与 Toast 文案**(`COMPLETE_FIRST_UNIT`/`WAKE_MAI`/`EAR_SOAK`/`DAILY_*`/`WINBACK_LESSON` 等)。⚠️ 硬缺口已入 pending:**优先级方向正文与后台相反**、**回归任务「连续 3 天」文案与「30 天内累计 3 天」规则矛盾**、**「任务 1.0 数据如何处理」正文自己留白**、**全篇无埋点明细**。本篇待确认 17 项。⚠️ 另:本篇 img-01(14246×2692,缩放 13%)按规则判定不可读、**未读取**,Onboarding 流程细节需看 Figma;img-05(缩 16% 存疑)只辨认出屏级结构,小字未读。
- **2026-09-08 新增 ingest(1 篇 V1.38.0)**:`IP打电话 / 电话机与来电互动`(**新建一级模块 IP打电话**)——《IP打电话增加孩子亲密度》,AB key **`reward_get_calls`**:完成**第二单元**路径掉落「电话机」道具,首次退出电话机页后**飞入奖励中心固定家具位**;三个应用内接听场景(**电话机页停留 20s 未拨 / 首页停留 5s 或完成单元点勋章 / 家园停留 5s 且非换装非装扮态**),**一天只来电一次、当天已拨通则不再来电**;主动拨号 = 通讯录号码 + 拨号键、**铃响 3s 接通**,显示屏两位槽满位后再按会整体清空重填;**连续两次拒接/中途挂断 或 未接语音 >7 条未消费 → 推【取消电话订阅】话术后停推**,按 1 可重新订阅、该留言不消费不消失;**播放达音频 90% 时长才算完整接听**;未接内容按时序进语音信箱(**只播问题音频、不播回复音频**);**每晚 20 点**判断当日未接通则发 push、按 IP 取不同文案;内容侧首期 **20–30 条音频**、7 个 IP 轮转消费(**与用户是否来 App 无关**),脚本结构 接通/自我介绍/提问/反馈/挂断,**AI 只答一轮、超 5s 用兜底音频**(埋点表标为二期)。埋点 18 个事件(`calls_*` / `voicemail_*`,含后端事件 `calls_unsubscribe_trigger`/`calls_push_send`)。⚠️ **原料重抓说明**:该篇 2026-08-24 抓过旧快照(正文 2424 字符/6 图/缺内嵌表,且第 3 张 sheet token 已失效),经主人确认用 `--force` 重抓,当前原料为 4404 字符/8 图/5 张内嵌 Sheet。⚠️ 硬缺口已入 pending:**首次来电等待时长正文 20s vs 验收表 3s**、**停止来电条件正文与验收表两套**、**「装扮后才触发被动来电」只在验收表出现**、**通讯录号码正文表/设计稿/正文示例三处对不上**。本篇待确认 17 项。
- **2026-09-08 新增 ingest(2 篇 V1.38.0,V1.38 起头)**:① `奖励中心 / 商城购买`——《奖励中心拓展空间提升购买率》,一篇里塞了四件事:**新人低价套装专属标签**(Admin 新增「新人套装」标签枚举,只对**未购买任何商品**用户曝光、排列表最前、样式是绿色 % 角标、买任意一件即消失)+ **全量上新 New 标签**(首次曝光起算,后台配时长、**默认 7 天**、下架重上重新计时)+ **两层排序**(第一层后台权重、第二层按**过去一周购买的性别属性占比**,仅对**购买 ≥2 次**用户生效;套装新增**性别字段 男/女/中性**;后台改为**拖拽排序 + 预览图**)+ **家具面板**(购买过服装后再进奖励中心**自动打开**,布局从右侧改**下侧**、场景 0.9x→0.7x)+ **新空间场景「浴室」**(Giggles **>500 弱提示小标签 / >1000 强提示弹窗**,`Unlock Now ⓖ1000`,解锁后平移动画 1.9s + 首次左右滑动引导 3s/次,数据结构新增**空间场景 → 家具主题 → 家具物品**三层,老主题上线后配为「起居室」,**串门进入的仍是起居室**);埋点 13 个事件(`reward_tag_expose`/`reward_list_expose`/`furniture_panel_*`/`space_unlock_*`/`space_swipe`),购买成功事件**只加字段不新建、事件名待研发确认**。② `同桌 / 组队邀请与游戏挑战`——《邀请同桌 1.3 支持随机匹配》,给同桌加第三条组队路径 **Random Match**:候选池 = 未配对 + **年龄 ±2 岁** + 排除自己/已在 Pending 的人,推荐顺序 **在线优先(心跳方案待定) → 24h 内活跃 → 随机**,**一次只推 1 位**、Not This One 换人不重复推、Team Up 二次确认后**不可撤回**、**最多同时挂 10 条**,Pending 列表按申请时间倒序、四态文案待定,受邀方在同桌页弹窗、不在则站内信 + Push(**每 12 小时最多一条**),被拒/超时**无提示**;埋点 11 个新事件 `studybuddy_*` + 现有 `deskmate_pair_success` 加 `pair_source`(share_code/join_code/random_match)。⚠️ 两篇硬缺口已入 pending:**新空间「Giggles 不足」正文写 toast、设计稿是双按钮弹窗**;**随机匹配正文写「不展示列表」但设计稿是带筛选 tab 的候选人列表页**;**受邀弹窗标签数量正文与设计稿不一致**;**申请超时时长全篇未定义**。两篇待确认共 26 项。
- **2026-08-24 新增 ingest(3 篇 V1.37.0)**:① `学习路径 / 路径分支与等级配置`(**新建二级**)——《路径双分支与搜索模块角标适配》,是《单元内连续学习》里「自然拼读体系拆出来」的**后台+数据配套**:Admin 路径等级配置新增**主课程/自然拼读双分支 Tab**,两分支**各有独立 Level number 命名空间**(主课程 `Level n`、自然拼读 **`Phonics n`**,编号**分支内唯一**、名称自动带出且手改后停止联动);App 搜索角标按分支渲染 `Lv n`/`Phonics n`,同相关性排序改为**主课程>自然拼读>同分支 Level 低→高**;⚠️ **所有带 `Level_Number`/`speech_level_number` 的埋点统一加 `branch_code`(main/phonics)+`branch_number`** —— 原文列了**约 400 个事件名**+34 个必测清单,影响面覆盖全业务线。② `语音识别 / 超时提交与分年龄阈值`(**新建二级**)——两个判定修复:**UI 倒计时结束前主动截停一次**把已收音频交引擎出分(解决「读对了但没停下来→引擎等尾静音→判超时」);**打分放行阈值按年龄分三档**(T1 <4 岁 基线−Δ₁ / T2 4~6 岁 基线−Δ₂ / T3 ≥7 岁或异常值 基线不变),**Δ 值由 wheat 定、未给数值**。③ `新用户引导 / Onboarding语言选择`——**下架 V1.36 的 `onboarding_chinesecourse` AB 实验**:选年龄页改回历史版本、新增孩子去掉选语言;但**菜单页保留切换语言进中文小麦**(印尼语/中文/英文,语言基于设置中的语言)。relate 已正确连上 V1.36《Onboarding 流程引入中文》与《小麦中文课程》。⚠️ 三处硬缺口已入 pending:**双分支篇「课程分支创建后可否修改」正文与设计稿直接矛盾**、**阈值篇 Δ₁/Δ₂ 无数值且全篇无埋点(核心目标无法验证)**、**下架篇没写 V1.36 五项里另外三项是否一并下架、也没写存量中文用户如何迁移**。三篇待确认共 31 项。
- **2026-08-24 新增 ingest(4 篇 V1.37.0)**:① `同桌 / 组队邀请与游戏挑战`(**新建一级模块同桌**)——《邀请同桌 1.2 支持游戏挑战》,即《游戏大厅 2.0》里说的「Study Buddy 单开需求」本体:发起页改**每 3s 切换的四条奖励轮播**、分享从「只有海报」改为 **WhatsApp/微信/复制链接三渠道分享 H5 邀请链接**(需原生改分享组件);组队成功页 **VS 换成宝箱**(双方都完成同一 Level 后可点、**一人获得 50 Giggles**)、进度条改当前 Level 进度、底部新增**「我/同桌最近在玩的 3 个游戏」并可挑战对方最高分**(5 款游戏、复用 PK 动画与挑战课结局动画);⚠️ 背景漏斗显示**真正卡点是「登录，90% 用户流失」而本期没改**;1 内嵌 Sheet 装着 6 个埋点已补抓(**`studybuddy_*` 与 `deskmate_invite_share` 前缀不统一**)。② `游戏模板配置 / 单词展示模式`——新增标准字段组件 **`WordDisplayMode`**(仅图片/仅单词名/图文随机混用),**一课一值、作用于全部词槽**;主题侧只勾选有无该字段,取值在课程配置页选,**默认值与抽签算法写死端上包内**;联动词卡「原画图片」必填态(**TEXT_ONLY 切换时不清空、仅放宽校验**) + Generate 缺图阻断;打包 JSON 加 `word_display` 节点。③ `语音识别 / 故事书跟读组件`——《故事书跟读模式以及完课结算页/分享海报优化》:因新内容会出现**同页两音色 + 同页多句**,**跟读单元从「图页」下移到「句」**(进入时一次性构建可跟读句序列,推进/进度/分母/星级/封面回放全基于它;**同页切句不换图不播翻页动效**;**每句仅存最终通过录音**;封面回放**同页句间 0.2s/跨页 0.4s**);结算页**仅故事书模块入口**新增同系列下一本推荐位(**路径入口不动**);分享海报从品牌海报改**孩子的「跟读作品卡」**(书封+谁读的+成就+「扫码听 TA 读的这本书」二维码)。原文给了**完整新旧口径对照表 + 12 条验收标准**。④ `游戏模板配置 / 路径游戏模板与难度`——4 款新游戏(点名连连看/托盘打包台/钓鱼钩卡/听音选门跑酷)接进路径 Game Template,**3 款支持四档难度**;Admin 路径与 Miniplan 新增 **`difficulty`** 下拉(**仅 3 个指定模板展示、默认中等、未选模板时字段不存在而非 disabled**);6 个 `app_game_*` 统一加 `difficulty`(含**新增事件 `TappingWithNoResponse`**),`app_enter_quiz`/`app_complete_quiz` 的 type 与 scene 新增枚举 **`Gamelist`**。**本篇含四款游戏的完整玩法规则与全部难度参数表**(反乱按机制:灰石/托盘格/海藻占钩位/人群×留人比例)。⚠️ 两处跨篇冲突已入 pending:**③ 的「点推荐位不再二次选模式」与《故事书使用方式引导》的「每次打开都弹模式选择」直接冲突**;**④ 的四档难度与《游戏大厅 2.0》介绍页的三档(Easy/Medium/Hard)不一致**。四篇待确认共 49 项。
- **2026-08-24 新增 ingest(V1.37.0)**:`消息推送 / 推送规则与文案迭代`(**新建二级**)——《Push 迭代》:**现有推送全部改为游客也推**(此前排除游客导致触达不到大量用户;技术优化 1.36 先上)、时间统一为**用户本地 19:00 或自定义提醒时间**;每日正学提醒改为**五级取一条**(**P1 连胜特殊节点[3/7 倍数带奖励、10 倍数里程碑] > P2 小麦[昨日有对话] > P3 故事书[昨日读完一本] > P4 Giggle TV[昨日听完一首] > P5 课程类[30 天内不重复]**),且「连胜/小麦/storybook/gigglesong/新人引导命中后当天不推课程 push」;**印尼当地 14:00 单独推儿童兴趣内容**(仅印尼语用户,PK/游戏大厅/邀请同桌/家园装扮四类);挽留召回改为**缺席 day2 的 14:00+19:00、day3–day7 每晚 19:00**;新人 Onboarding 维持 AB 只微调文案(**统一加 Tap 明确动作**);**下掉连胜保护卡 push**(已不再发连胜道具)。⚠️ **本篇是「正文短、内容全在表里」的最极端案例**:正文仅 1228 字符、**0 图 0 正文表格**(`meta.json` 的 `tables: 0` 严重误导),但有 **9 个内嵌 Sheet 装着约 100 条 push 文案**(含 44 条 × 8 档位的课程类文案表),已全部补抓落 `raw/Push-迭代/sheets.json`。⚠️ 两处硬冲突已入 pending:**同一节里两套 P1–P5 优先级编号含义完全不同**、**14:00 印尼推可能撞 V1.35 定下的祈祷时段禁推表**;另 **Push 链路埋点章节为空**。18 项待确认。
- **2026-08-24 新增 ingest(2 篇 V1.37.0)**:① `竞技场 / 游戏大厅与小游戏`(**新建二级**)——竞技场升级为 **Game Arcade 游戏大厅**:依据是 PK 在**非首日用户**上留存全面优于对照组(次日 +0.37pp ~ 第6日 +0.86pp、人均 3.7 次;新用户首日 PK 无提升);聚合入口新增 Game Arcade **替换家庭游戏入口**(前置:`homepage_toplearners` 实验组全量,同时 Deskmate 回第一排末位、排行榜移首页右上第二位);上线 **6 单人 + 2 本地多人**小游戏(单词PK / 无尽模式[3 分钟、错 3 次结束、**下掉开扭蛋**] / 点名连连看[难度=4·8·16 词] / 托盘打包台 / 钓鱼钩卡[**排名单位=米**] / 听音跑酷[答对人群×2、答错×0.7、巨人天降] / 单词飞船 / 抢答[单词·百科1000题·100 以内口算]);**每局统一 20 Giggles**、单人游戏各自独立**全国/全球榜前 100**(上榜高亮、未上榜置底、榜单不共用);分享走 **Open Graph 免登录试玩落地页**(og:image 1200×630、无审批、吸底下载按钮走 adjust `21pj288c`)。⚠️ **本篇无埋点方案**,只有 3 条开发提问(分享按钮无埋点、单机游戏三入口不区分来源、埋点冗余),玩法细节全在外部 6 份 PRD md。② `Giggle TV / 童锁与自动播放`(**新建一级模块 Giggle TV**)——聚合入口 **Songs 改名 Giggle TV**;播放页右上「更多」提供 **Cast to TV + Child Lock**,开童锁后**控件全消失 + toast `Child Lock is On`**;播放结束**重播 + Up Next 后 5 个视频 + 5s 无操作自动续播、不足 5 个取最早补足、整列表循环**;**投屏放下一期**(走**系统自带投屏** + Oppo/Vivo/小米/华为/苹果/三星 六品牌教程页),设置-屏幕时间底部加 `To protect your eyes, try casting to a TV for learning.` + `How to cast?`。⚠️ 1 个内嵌 Sheet 装着**全部 5 个埋点**(`giggletv_tab_click`/`cast_click`/`cast_guide_view`/`childlock_toggle`/`end_screen_click`)已补抓;其中 `giggletv_tab_click` 的「**推荐/专辑 tab**」正文完全没有需求描述 —— 「算法推荐」这条分发方式疑似漏写。两篇待确认共 27 项。
- **2026-08-24 新增 ingest(3 篇 V1.37.0,小麦专场,全部无图无内嵌 Sheet)**:① `AI Tutor / 新用户破冰仪式` —— 《新用户唤醒仪式优化》:V1.35 上线实测 **start→woke 两天都卡在 ~65%**、main_chat 0 轮率仅 -2.5pp/-1.2pp,本期把两步合并为「进场即打鼾 → 点击触发打哈欠动画+音效(降分贝防自唤醒) → 4 条递进循环文案 → Combo 连击」,唤起规则**禁止点击与小麦自身声音达阈值**(删除吹口哨)、高脉冲但**未达 0.1S** 给循环提示,新增 **ASR 解析「Hi」累计 3 次的小音量兜底唤醒**;目标 65%→85%。② `AI Tutor / 作息陪伴与冻结解救`(**新建二级模块**,9569 字符 / 14 张表,信息量最大)—— 给小麦装宗教/自然作息:`rhythm_mode ∈ {sun_based, prayer_based}` + `probe_status ∈ {active, done}`(**字段不含宗教词以过合规线**),prayer_based 用 Kemenag 口径 + **城市级 IP(不请求 GPS)** + 每日拉取 7 天缓存、缓存过期退化 sun_based(**礼拜时间零容忍**);打标 = 系统探测(**仅印尼语**)+ 宗教生活信号主动打标(**第一人称 ≥1 次且跨 ≥1 自然日**,所有语言适用);进入走**七级承接矩阵 P1 冻结解救→P7 常规**(带真实命中比例:匆忙折返 41.8%、常规 26.1%、当日重访 14.0%、冻结各档 9.8/4.0/2.2/2.1%);**Day1–6 渐冻 → Day7+ 冰雕**,解救 = 锤击 **combo × 不活跃天数**(冰雕恒定 7,退出解救流程才清零);承接后**≤2 轮内必须导向 word play**;约定系统单槽、新覆盖旧、过期静默作废;附录给了可直接交付的 **Demo 规格 + C1–C14 用例清单**。③ `AI Tutor / 场景内口语对话课` —— 《M Town Talk 功能优化》:小麦从单向解释改多轮交互(**激活最高优先级**,三触发:连续两轮开麦无进展/点画面内容/主动点小麦),Prompt 改**反问引导 + 母语沟通**、胶囊可关闭拒读(**仅中英文**)、点 object 播动画+音效(动画在音频周期内循环、重复点从头播、支持多 object 同触发、**音效播完才进下一轮对话**),**动态 prompt 把用户英语水平反馈给 NPC 改其说话习惯**(太难→简单重复慢速/太简单→更有趣复杂);多镜头切换**本迭代仅技术预研**。⚠️ 三篇共性缺口:**全部无埋点或埋点章节为空**(作息篇只写了「埋点和参数参见:」)、**版本号字段全为「(待填)」**、**全部无图**(视觉只在 Figma/外部 demo);另有两处跨篇矛盾已入 pending:**轻唤醒可点击 10 下唤醒 vs 唤醒仪式禁止点击触发**、**「小麦睡着」在三篇里三种定义**。三篇待确认共 32 项。
- **2026-08-24 新增 ingest(5 篇 V1.37.0,批量)**:① `反馈与社区 / WA推送体系接入`——游客菜单加【联系我们】(带家长验证)、APP 与 WA 按 KIDID 绑定、回「我是家长」后进**按首次互动 +18h 累计的 4 轮推送**(每轮按当天有无完课走报告钩子/功能钩子两套文案),打标签同步 User ID/Country/Language/Child Age,10 层漏斗(③加好友转化与⑤送达**只有 WA Business API 能看到**),新增 10 个服务端/深链埋点(`wa_kid_binding_*` / `wa_push_send`[push_round=first_18h~day4_18h、branch_type=completed/not_completed] / `wa_reply_option_select` / `wa_unsubscribe` / `deeplink_*`);**本期实测 13 个 App Scheme 外链**——走 `unity/customsendmessage` 的 6 个入口仍只落主路径、练习册 QA 白屏(与 V1.35 同一遗留,未修)。② `故事书 / 阅读模式与播放器设置`(**新建一级模块故事书**)——进故事书即弹模式选择(原声 Read/讲解 Guided/录音 Record/小测试 Quiz,**Listen 已于 08-11 改名 read**),给出完整「播放模式 × 操作」矩阵(双语字幕仅原声+讲解、磨耳朵仅原声、录音走自身界面),GiggleCast 新增**单曲/列表循环**与**原声/母语音轨切换**(三个前置条件),播放器记住设置但**播放模式不记忆、每次都要选**;埋点 `StoryBookStart|Complete` 加 play_mode/subtitle_on/page_mode + **`sentence_depth`/`total_sentences` 句子完成率**,新增 `gigglecast_loop_mode`/`gigglecast_native_mode`。③ `学习中心 / 单元卡片介绍`——补 Lv2～Lv9 单元卡点击无反应的缺口,弹介绍卡(名称/标签/介绍/核心单词可播音/关闭回首页),**标签从写死改为后台 `unit tags` 可配**(Curriculum Paths→Edit Unit 新增区块),并**下掉学习中心单词卡的旧学习路径入口**(即①篇提到的 Leah 需求本体)。④ `反馈与社区 / 调研问卷`——家长中心看板**轮播广告位**(1 号位问卷、登录+游客都展示)进 App 内问卷,5 种题型(单选/多选/评分/问答/录音),后台三步向导 + **三态状态机**(未发布/已发布/停止发布) + CSV 导出(UTF-8 BOM,录音存文件),多语言只录英文源走 Tolgee;⚠️ **需求范围写的是 Google 表单 + 手动发 100 Giggles,功能描述却是自研 H5 + 自动发 N Giggles,两套方案未拍板**。⑤ `挑战课与Quiz / Quiz交互`——修**漏题 bug**:原来固定取线索单词文本,导致 L5 起字母认读题把答案字母印在提示区(家长群实拍);改为严格按 **Clue 类型渲染**(本期 Text/Audio/Text+Audio;None 与 Image 系走兜底不报错),**字母题=Audio 只给提示音**、单词题=Text+Audio;上线时**可回滚的批量迁移**(规则1 已启用全置 Text+Audio → 规则2 线索词为字母的覆盖为 Audio → 音频缺失告警拦截不静默上线 → 抽样验证)。⚠️ **本批 2 篇有内嵌 Sheet**(WA 6 张、问卷 4 张)已补抓落各自 `sheets.json`。5 篇待确认共 40+ 项已入 pending-confirmations。
- **2026-08-24 新增 ingest(V1.37.0)**:`任务体系 / 任务面板与任务列表`——**新建一级模块任务体系**。首页加任务面板入口,用「任务→奖励」把用户推去体验没碰过的核心功能;新老用户各一层 AB(`homepage_tasks_list` / `homepage_tasks_list_old`,按**游客 id 是否 8/21 前生成**划人群;新用户实验组 5 个任务、老用户 2 任务+邀请兜底且**不管过去是否完成过都可再引导**)。5 个任务:P1 完成第1单元(单元勋章发放后判定,奖一套服装)、P2 唤醒小麦、P3 打开消息通知(200 Giggles)、P4 跟读故事书一本、P6 邀请好友(100 Giggles 无上限,**缺 P5**)。共性:任务间**无前后依赖**、**自主探索也能被动完成并给奖**、完成后收纳进「已完成任务」列表、领奖走 `New Award`+`Take it` 后飞入奖励中心;邀请卡片**没有已完成待领取/收起态**。后台需给奖励「获取方式」加枚举**任务奖励**。⚠️ **本篇正文仅 3880 字符,核心埋点表 + 三语文案表全在 3 个内嵌 Sheet(`block_type=30`)里**,已用 sheets/v2 API 补抓落 `raw/任务体系/sheets.json`,并在 `meta.json` 加 `embedded_sheets` 字段(tables=5 只算正文表)。埋点:`tasksicon_*` / `taskslist_*` / `taskcard_expose` / `task_complete`(带 `complete_source`=panel_guided/self_explore) / `task_reward_claim` / `wheat_feature_*` / `push_permission_result` / `storybook_readmode_entry`,外加 `task_story_intro*`、`task_mai_intro*`、`app_invite_*`,以及既有 `app_popup_push_show|request` 的 `scene` 新增枚举 `tasks`。十二项待确认已入 pending-confirmations(最硬:**小麦命名 wheat/mai/Xiaomai/Mai 四套并存**、缺 P5、老用户 2 还是 3 个任务)。
- **2026-08-24 新增 ingest(V1.37.0,新版本起头)**:`学习路径 / 单元内连续学习与单元结算`——新建二级模块**单元内连续学习与单元结算**。两块内容:① 在 V1.35 `homepage_learning_order` **实验组**上**不新开 AB** 叠加「完课中间过渡页(打星+Giggles 动画搬到过渡页、下一节未完课自动进/已完课不进、中途退出定位当前未完成课、连到单元结算则定位单元勋章)」+「单元结算页两态(有开口=开口/互动次数+孩子录音回放+单词开口 Nx;无开口=只有互动次数+教学音频单词卡)」,开口次数口径 = `lesson_speech_recognition_end` 且 `is_speech_detected=true` 的条数,靠 `speech_level_number`/`speech_unit_number`/`speech_part_number`/`speech_part_id` 定位维度,同词多次跟读按评分刷新存最高分音频;② **全局不 AB**把自然拼读从主课程 L6–L9 拆成独立主题(主课程收窄 L1–L5、新 L6–L9 挂 coming soon),学习中心课程介绍改主课程/自然拼读双 tab,已解锁 L6+ 老用户冷启动弹「Main course updated」弹窗(**优先级高于其他弹窗、无 X 关闭只能二选一**),并要求回归四类奖励/进度数据(奖杯改判「是否完成当前 Level 升级 Quiz」、宝箱扭蛋证书按后端 unit ID、单元勋章会因课程变动刷新、单元套装看是否已获得)。新埋点 `learn_continue_*` / `unit_summary_*` / `unit_barge_*` / `phonics_teaching_*`。⚠️ **本篇跨模块**(Phonics/学习中心/奖励系统/奖励中心/离线/单词卡),`--module 学习路径` 之外还需用 `phonics`、`自然拼读`、`unit_summary` 关键词兜底。七项待确认已入 pending-confirmations。
- **2026-08-11 新增 ingest(V1.35.0)**:`消息推送 / 新用户push优化`——新建一级模块**消息推送**(二级:新用户首日Push、Push下线与冲突治理)。内容:D0(14:00/18:00 AB)+ D1(8:00/14:00 AB)新用户定时 push、UTC+8 只推中国印尼、印尼时段禁推表(TPA 古兰经班/祈祷时段绝对避开)、极光后台 38 条 Plan ID 触达点击表(0 触发标"疑似 bug")、19:00 时段冲突治理(lesson_incomplete_day3/day4 各保留 b 变体;标识名与 Tolgee 文案 key 差一天)。relate 连上「终身学员计划增加Push」(其 `lifetime_student` 正是本次下掉项)。⚠️ 两项口径不一致(AB 组条数、下掉范围 6~24)已入 pending-confirmations。
- ✅ **fetch.py 已支持内嵌 Sheet / Bitable 抓取(2026-08-28 修复)**:此前 `block_type=30`(sheet,token 形如 `<spreadsheet_token>_<sheet_id>`)与 `block_type=18`(bitable,`<app_token>_<table_id>`)的内容既不在 `content.txt` 也不在 `blocks.json`,整段漏掉(实测有正文仅 402 字符、埋点表与三语文案表全在内嵌表里的需求文档)。现由 fetch 统一抓进 `raw/<slug>/sheets.json`,`meta.json` 增 `embedded_sheets` / `embedded_bitables` 计数(`tables` 仍只数正文表格,勿再用它判断"有没有表")。实现要点:先读 `sheets/v2/.../metainfo` 拿真实行列数构造精确 range(旧临时脚本固定 `A1:Z300`,宽表会横向截断、空网格会填出成片 null);取值加 `dateTimeRenderOption=FormattedString&valueRenderOption=FormattedValue`(否则 14:00 落成 0.5833、70.60% 落成 0.706);富文本段数组 / 图片 dict / @人 dict 压平为纯文本 + 链接;裁掉尾部整行整列空白。**遗留**:37 篇含内嵌 Sheet 的存量文档里仍有 20 篇从未补抓,已补的 17 篇是三种不同的临时 schema —— 待统一回填。
- **2026-08-11 新增 ingest(3 篇 V1.35.0)**:① `排行榜 / 排名动效与Push提醒`(新用户首日进 Top 5 才曝光、第 1 名专属动效[优先级 第1名>超越>无]、排行榜退出拦截弹 Push 引导[每周 1 次]、被超越/周日 14:00 两条 push、第 11–30 名冲刺气泡、昵称中英印三语敏感词审核 + 存量清洗、`app_overtake_animation_show` 新增 `rank_status`);② `学习路径 / 解锁规则与学习顺序`(**新建一级模块学习路径**;在 V1.34 `homepage_reward_giggles` 实验组之上叠加第二层 AB `homepage_learning_order`:按完成单元一次解两个单元、单元内顺序学单元间可跳、Hello 课流程改造、新增 `app_unit_complete` 替代宝箱绑定上报、`status` 新增 `isOrderLocked`);③ `奖励中心 / 套装布局与体验优化`(4 件套 → 支持 5 件套 scale 0.9x、家具主题标签筛选 + 后台可配、6 条体验修复[家园进场动画性别/视角、首购 1s 延迟、家具引导退出条件、TTS 首次加载、换造型动画、拍照带家园背景]、新埋点 `reward_cabintab_switched`)。三篇各有待确认项已入 pending-confirmations。
- **2026-08-11 新增 ingest(5 篇 V1.35.0)**:① `竞技场 / 随机PK对战`(**新建一级模块竞技场**;异步单词 PK:每日 10 次、6 题听词选图+看图选词各半、20×剩余秒计分[前2s封顶160/后4s保底80/末题double/满分1120]、1.35 全机器人 1.36 换真人回放、PK 10 次开扭蛋、结果页三态、去掉功能广场邀请同桌入口;AB key `homepage_toplearners` 挂在排行榜实验上);② `竞技场 / PK机器人规则`(L1–L4 四档 P+T 参数、按连胜/连败/wr20 定档、仅 L1 有防碾压兜底、5 条硬性约束;**内容由 AI 生成,原文已声明**);③ `奖励中心 / 家园好友社交`(key `reward_visit_friends`:假访问数据 + 真实用户、强/弱提示+红点三态、Recent Visitors 回访列表、好友家园 say hi + 击掌/跳舞/比心微互动);④ `反馈与社区 / WhatsApp社群导流`(**Meta 账号被封,本期只上后台配置 + 跳转**,推送与打标签暂停;含**本期实测 12 个页面的 App Scheme 外链唤起结果**——走 `unity/customsendmessage` 的 5 个入口只能落主路径、练习册 QA 白屏);⑤ `学习路径 / 老用户路径迁移`(存量用户迁到 `819564819857413`、按 level+index 映射三规则、服务端标记 + 跨设备一次性弹窗、**弹窗优先级 迁移 > sunny家园 > 功能广场气泡**)。四项待确认已入 pending-confirmations(杀进程是否扣 PK 次数两篇冲突 / 家园假数据 24h vs 48h / 保留路径 2 条还是 3 条 / WA Scheme 缺口)。
- **2026-08-11 新增 ingest(5 篇 V1.36.0,Mai 专场)**:① `AI Tutor / 电量系统`(**成本控制**:电量=Mai 体力,main_chat + 全部子游戏共享;**双池**[每日 0.2 美金免费池 + Giggles 兑换池,先扣免费];**子游戏准入 ≥0.01 美金、进入后不打断、余额不可为负**;100 giggles=0.066 美金=10 分钟,**每日兑换硬顶 2 美金≈3000 giggles**;学习环节[greeting/word play/quiz]不计费;5 档电量表情 + **显示下限 0 上限 100**;严禁系统词);② `语音识别 / 变声器与录音卡片`(**6 种滤镜**刺激开口,**卡片手动关闭率 50–60%** 是要打的靶;说完自动提交 + 自动变声回放、切滤镜即重放;Collection 变"声音收藏册",**单词级记录原声+滤镜、不受卸载升级影响**);③ `AI Tutor / 中文课程`(2 岛屿 6 课程;**全局[Onboarding/电量/积分] vs 语言独立[背景/岛屿/Scene id/已学词]**;Mr.Dog→狗狗店长;猜汉字 word play[山人目];**中文不启用打分、用 ASR 文本匹配**;新增用户属性 `learn_language`);④ `AI Tutor / 场景内口语对话课 - bbox 点击`(把乱点当意图喂给 AI:三场景×双角色话术、区分主对象/附属道具、**排除 NPC**、不进 Collection 不判对错);⑤ `语音识别 / 语音组件一致性`(气泡样式统一[最高 3 行、跟随 TTS 滚动、**不再实时高亮**]、M Town Talk 静态脚本支持 srt、自由麦统一[点击启动/再点或超时发送 + 安静时红块 Scale1.1 跳动]、单词染色接入)。五项待确认已入 pending-confirmations。
- **2026-08-11 新增 ingest(6 篇 V1.36.0,新版本起头)**:① `竞技场 / 真人PK数据接入`(1.35 采集的真人数据按 P25/P50/P75 分四档,**某档记录 >100 条才随机取真人回放、≤100 仍打机器人**,不取本人记录);② `账号与设置 / 注册登录`(**全新邮箱+密码免验证码直接注册**、按 IP 突出 Google/微信、**安卓取消 Apple 登录**、账号页 Verify Email 补验证、邮件确认 H5 三态[confirmed/expired/unbound]);③ `新用户引导 / Onboarding语言选择`(**新增中文课**:Onboarding 插语言选择页、**中文锁 Level 1**、中文首页直接是小Mai、家长中心可切语言、每个孩子语言进度独立);④ `奖励系统 / 开口激励扭蛋`(开口徽标 x/N 跨课程累计、**门槛序列 Hello 3 → Color 4 → 第3轮起固定 5**、完课达标开扭蛋出家具);⑤ `课程加载 / 离线模式`(离线主页改**资源 tab 分类**、歌曲/磨耳朵新增离线下载[需登录、不可暂停]、设置-Downloads 资源管理[单个直删/Delete All 二次确认]、**离线完课不发奖励不播结算动画**、已有事件加 `linemode`);⑥ `排行榜 / 连胜打卡榜`(竞技场排行榜新增 tab、**全球前 100 名**、天数→百分比 14 档位表、上榜高亮/未上榜底部固定条 999+、连胜动效去掉冰封道具、Continue 后接排行榜动效或回首页 3s 自动进下一关)。五项待确认已入 pending-confirmations。
- **2026-08-11 新增 ingest(V1.35.0)**:`语音识别 / 故事书跟读组件优化`——把课程侧 1.33.0 跟读体验搬到故事书:**全流程自动**(领读→自动录音→判定→回放录音→停 0.5s 自动翻页,通过路径零点击)、**取消全部人声/TTS 反馈改动画**(避免孩子把 "Excellent" 当正文跟读;缩小版 Max 动效 7 种随机、连续 3 次 2 星+ 触发全屏庆祝)、通过后回放孩子录音(头像 scale 1→1.1→0.0→1)、失败自动重领读、**同页失败 ≥2 次翻页键高亮**、音量条改真实音量驱动、**完课结算页封面回放录音合集 + 纯封面+音频分享**、弱网/短句路由自研模型。mic 按钮职责收敛(录音中=提前提交、领读中=提前开始录音),根除双击空录音。
- **2026-08-11 新增 ingest(6 篇 V1.35.0,Mai/语音专场)**:① `AI Tutor / 新用户破冰仪式`(睡着→麦克风检测→唤醒三态;**RMS 声控唤醒不做 ASR**、麦克风硬依赖不可跳过;背景数据:首日 0 轮层 21.8%、D1 仅 8.9%、91% 次日消失);② `AI Tutor / 新内容更新引导`(老用户闪光引导:岛屿+Scene 卡闪光、猜词后语音提示、Admin 按 user ID 手动推送;`StartTalkScene`/`EndTalkScene` 加 `is_new_content`);③ `AI Tutor / 全局单词本Quiz`(已学词 ≥5 从游戏回主路径触发、一天一次、3–10 题单选、闭麦点选、每题 3 次机会、`MaiQuiz*` 埋点);④ `AI Tutor / 模型切换配置`(TTS 豆包/千问切换、ASR 打分切自研、印尼语分桶、`tts_provider` 埋点;**全文仅 506 字符,分桶规则与容错全缺**);⑤ `AI Tutor / 场景内口语对话课 - M town talk 流程优化`(关卡选择页替代完成页、首轮严格四步不变、重玩自选甲乙方、tutorial 阶段流失率 50–60%);⑥ `语音识别 / 音量阈值设定`(论证型文档:App 测不到真 dBA,改用 SDK 相对振幅 **Noise 8 / Speech 15 / Reading 25–35 / Very Loud 70**;与①的 RMS 0.15–0.18 是同一阈值的两种量纲)。五项待确认已入 pending-confirmations。
- **2026-07-13 新增 ingest(V1.34.0)**:`语音识别 / 自适应跟读计时与会话统计`(跟读固定倒计时→开口宽限 6s + 动态录音窗[6s+0.5s×(音节数−1) 封顶 10s]、lesson_speech_recognition_end 新增会话级 session_id/最终结果字段并移除 speech_skip、VAD 降噪;⚠️ 字段命名文档内不一致已入 pending-confirmations);`AI Tutor / 学习场景屏幕常亮`(故事书图文播放器自动播放、AI Tutor 对话课全界面进入即开屏幕常亮,离开恢复系统默认;无图短文档;relate 连上 AI Tutor 模块全部 5 篇)。同日另有会话入库 Mai场景内口语对话课、Mai引导交互优化、语音识别-闪卡跟读与音素打分、音频库-课程语音生成管理优化(详见 notes/)。
- **2026-07-13 新增 ingest(3 篇 V1.34.0)**:① `奖励系统 / 路径奖励AB测试`(路径奖励 AB 实验:实验组砍扭蛋宝箱、走强 Giggles+弱勋章、新增 Giggles 购买引导弹窗、单词库入口迁进学习中心);② `奖励中心 / 商城购买 - 奖励新增购买服装和家具`(夏日海边主题服装 + 新家具购买流程改版[使用/Place it、拖拽放置]、My Giggles 积分明细页、邀请同桌获取服装);③ `挑战课与Quiz / 跟读挑战课`(新测验类型:语音识别判定开口读词、Max/Lily 友谊赛对战外壳、龟派必杀、走 gamelist 模板+语音识别埋点、三正向结局)。relate 分别连同模块历史篇,③ 还连上语音识别「跟读组件改版」;跨模块强关联(学习中心/首页弹窗/终身学员计划等)靠关键词检索兜底。
- ✅ **知识库瘦身 第二轮:WebP q80 归档(2026-08-28)**:**388MB → 81MB(其中 .venv 占 22MB,知识库本体 59MB)**,图片 394MB → 41.5MB(省 89%)。① **格式选型经实测**:AVIF 虽然大图上体积只有 WebP 一半,但**读图工具不认**(读出来是二进制乱码)→ 否决;WebP 实测可读。② **质量定 q80**,三种最坏场景真读通过 —— 直接读的密集文字截图、**随机验证码**(`GIGGLE-M3Z8VJ6Q94QJ`,无法靠上下文猜)、有损+缩到1900px的叠加损失,读到内容与 PNG 基准完全一致;q90 大 20% 而最坏偏差只差 7/255;q60 也能读但全库只多省 8MB(压缩收益几乎全来自 PNG→WebP 这一步,q 值 60–80 区间是平的)。颜色语义安全:大偏差全是文字边缘振铃,平坦区最大偏差 ≤39/255、平均 <1/255。③ **9 张不可读图降级为 1900px 占位**:它们占压缩后总体积的 71%(53.2MB),读都读不出,留原图纯属浪费。④ 新增 `scripts/compress_images.py`(项目级 `.venv` + Pillow,**只此一个脚本有依赖**,fetch/read_cache/search 保持零依赖)。⑤ 派生副本改为统一 `.read.png` —— `sips` 能读 WebP 但**只能写 PNG**,这样 read_cache 保持零依赖。⑥ 校验:704 张全部可打开、meta 指向 704/704 存在、笔记 373 处图链接零失效、转换后真读复验通过。
- ⚠️ **事实来源口径已变更**:本地不再是无损归档,而是 **q80 有损缓存 + 不可读图占位**。真正的事实来源是**飞书**:`blocks.json` 存着 image token(实测 2026-06-30 老文档至今可重下),`meta.json` 的 `original`(file/bytes/md5/dims)可核验重下的是不是同一张。主人 2026-08-28 明确决定不做「raw 移出 git」,所以 `.git` 仍保留全部 PNG 历史(276MB),本次瘦身只作用于工作区。
- ✅ **知识库瘦身(2026-08-28)**:538MB → **385MB(省 153MB / 28%)**,能力零降级。① **删掉 `.read` 读图缩放副本**(277 张 / 123MB,全库 23%)—— 它是 `sips -Z 1900` 从原图算出的 derived 数据,实测往返(清理→重建)**md5 字节级完全一致**;已加进 `.gitignore`,平时不留存,要读超大图时 `python3 scripts/read_cache.py <slug>` 就地重建(路径与 `notes/` 图链接、`meta.json` 的 `read_file` 完全一致,**笔记和 meta 一个字没改**)。② **跨篇相同图改硬链接**(53 个冗余文件 / 28.7MB)—— 路径全保留、内容零变化(704 个文件 md5 逐个比对,0 变化)、读图行为不变;幂等,批量 ingest 后再跑 `scripts/dedupe_images.py --write` 即可。**评估后排除的方案**:整目录打包压缩只省 8%(PNG 已压缩);全量转 JPEG 省 55% 但 UI 小字会振铃、伤"读图转文字",属降级;图片降级为"按需重抓的缓存"可省 394MB(已验证 2026-06-30 老文档的图 token 至今仍可下载),但违反"原料层是事实来源"红线 —— **主人 2026-08-28 决定不做**;`raw/` 移出 git(可省 276MB 仓库体积)**主人决定暂不做**。
- ✅ **读图可读性判定(2026-08-28 修复)**:此前对超大图无差别 `sips -Z 1900`,把长条图压成 1900×290 这类细条,**模型只剩布局轮廓、一个字读不出,却照样写进了笔记** —— 实测老笔记《Top Learners 排行榜玩法》img-01 的描述「整体流程长图:完课 → 超越动效 → 排名上升 → Continue 回首页」是**看形状猜的**,而切片后能清楚读到「新人首次触发在第 2 课学习完成」「排名上升至前 3 名」及榜单具体名次 —— 触发条件与分支全丢了。**主人决定:不做切片,改为「读不出就不读」**。判据经四次真读标定:**只看缩放比例**(`1900÷长边`),13% 完全读不出 / 20% 基本可读 / 53% 清晰 / 91%(原图极扁但几乎不缩)可读 —— **长宽比和短边像素两个判据都实测证伪**。落地:`fetch.py` 新增 `readability()`(<15% unreadable / 15–25% marginal / 其余 ok),不可读的**不生成 `.read` 副本**、meta 记 `readable`+`read_scale`、fetch 摘要点名列出并要求笔记如实标注「未读取」;`read_cache.py` 复用同一函数(单一来源,避免规则漂移)。**前置硬拦截**:光靠 meta + SKILL 规则不够 —— 原图还在,读图工具会把它自动压到 ~2000px,压出来和糊掉的副本一样,后续会话直接打开原图照样会拿到糊图然后瞎猜;所以不可读的图**就地落 `img-NN.UNREADABLE.txt` 标记**(写明尺寸、缩放比例、判定依据、笔记该怎么写),`ls images/` 一眼可见,规则改为「见标记即跳过,连原图也不打开」。全库命中 **9 张不可读**(最极端 32768×5934 缩到 6%),分布在 6 篇:《【提前】新用户 Onboarding 增加产品价值传递》4 张、《Top Learners 排行榜玩法》2 张、《onboarding 流程去掉选择语言》《学习中心-课程介绍优化》《游戏大厅 2.0 小游戏》各 1 张。
- ⚠️ **存量待回补**:上述 **9 张不可读图所在的 6 篇笔记**,其图说明是在读不出图的前提下写的,存在**推断式描述**(看形状猜的),需要人工复核或改用其他手段(如让产品提供原始设计稿链接)。与「20 篇内嵌表未抓 + 17 篇旧格式」同属**已入库但内容不全**,待一并处理。
- **已 ingest 114 篇 / 33 模块**(覆盖 **V1.29–V1.38**;V1.35.0 已入 16 篇、V1.36.0 已入 11 篇、V1.37.0 已入 20 篇、**V1.38.0 已入 12 篇**;新增一级模块:IP打电话、闪卡;新增二级:单词回顾/每日复习、功能广场/鹦鹉学舌变声机;新增一级模块:消息推送、学习路径、竞技场、任务体系、故事书、Giggle TV、同桌):奖励中心(虚拟形象装扮/商城购买/家园装扮/渗透率引导)、奖励系统(单元宝箱/次日返场/Giggles获取/路径奖励AB测试)、终身学员计划(试学认证/AB分流/Push通知)、课程加载(预加载/离线模式)、新用户引导、反馈与社区(反馈收集/社群引导/应用市场好评引导/WhatsApp社群导流/WA推送体系/调研问卷)、挑战课与Quiz(单元挑战课/每日Quiz/Quiz交互/题目后台管理)、单词回顾、数据埋点(神策全埋点)、素材库(后台资源管理)、音频库(多语言音频中台)、AB实验平台、语音识别(麦克风授权/降噪增益/录音组件模式/跟读组件改版)、首页(布局结构/升级测试/弹窗优先级)、学习中心(课程介绍与级别进度/单元卡片介绍)、课程内容(Speaking跟读课/课程图标与口语标记)、AI Tutor(I spy游戏/Mai主对话/单图对话多玩法/场景内口语对话课/新用户破冰仪式/电量系统/中文课程/作息陪伴与冻结解救等 12 个二级)、账号与设置(孩子年龄/注册登录)、Phonics(主菜单入口与推荐路径)、排行榜(Top Learners学习之星)、功能广场(入口矩阵与曝光/新模块Admin配置/磨耳朵与睡前故事)、课程本地化(翻译开关引导)、游戏模板配置(单词资源字段/单词展示模式/路径游戏模板与难度)、官网SEO(SEO页面与下载转化)、消息推送(新用户首日Push/Push下线与冲突治理/推送规则与文案迭代)、学习路径(解锁规则与学习顺序/老用户路径迁移/单元内连续学习与单元结算/路径分支与等级配置)、竞技场(随机PK对战/PK机器人规则/真人PK数据接入/游戏大厅与小游戏)、任务体系(任务面板与任务列表)、故事书(阅读模式与播放器设置)、Giggle TV(童锁与自动播放)、同桌(组队邀请与游戏挑战)。全量见 `index/INDEX.md`。
- ✅ **版本冲突已确认**(2026-07-01):「Quiz对应多个技能标签」「Quiz手动创建题目」「Miniplan改Phonics AB测试」「语音识别降噪/增益」均按主人确认的 **V1.31.0** 入库;文档内 1.32.0 / v1.30.0 字段不作为本库版本。
- ✅ **音频库篇版本已确认 V1.31.0,无排期风险**(2026-07-01);标题/文档里的「延后」仅作为原文信息保留。
- **批量 ingest 验证(2026-06-30)**:一次投喂多链接 → 批量 fetch → **逐篇独立处理**(各自读全文+读图、独立落笔记)→ relate/index 只跑一次。逐篇独立 = 质量不降。素材库 21 图按规则只读 3 张关键大图(正文表格已覆盖字段)。
- ⚠️ **fetch 调用纪律**:`fetch.py` 输出量大(图多/正文长)时,**勿用 `| tail`/`| grep` 接管道**——下游提前关闭会让 fetch 收 SIGPIPE 报「exit 1」假失败(文件其实已写完)。改为重定向到文件再 grep。另:图多的篇(20+ 图)单次 fetch 可能超 2 分钟,需单独跑、给足 timeout。
- **relate.py 修过三类误连(均已入 STOP_IDS)**:① `iOS` 等平台词被驼峰规则误判。② `image_count` 等 fetch/meta 字段名(笔记写"无图"统一用「无图(0 张)」,勿写 meta 字段名)。③ **神策全埋点公参**(`page_name`/`page_title`/`page_path`/`page_id`/`element_name`/`element_type`/`scene`/`duration`/`position`)+ **通用内容生命周期事件**(`InteractiveLessonStart`/`StoryBookStart`/`app_game_play`/`app_page_view` 等)——只表示"涉及课程内容"、无功能区分度。**保留功能专属事件**(`app_enter_quiz`/`app_unit_reward`/`word_review_*` 等)的关联价值,故"挑战课↔预加载"这类有意义连接仍在。
- **fetch.py 加固(2026-06-30)**:① **原料层禁止覆盖**——`raw/<slug>/meta.json` 已存在则拒绝重抓(对齐红线),确需重抓加 `--force`;半成品(无 meta)可直接重抓。② **raw_content / blocks / 图片下载任一失败即非 0 退出**,不再产出空正文、缺结构或缺图的坏原料。③ **网络层异常(SSL 握手超时/连接失败)不再让脚本崩溃**——`http()` 捕获 URLError/OSError 返回错误码;**图片下载自动重试 3 次**(应对网络抖动,常见于 10+ 图大文档),3 次仍失败才中止。
- **待确认项集中维护**:`docs/pending-confirmations.md` 持续累积(最新一条为 2026-08-24 V1.37.0 单元内连续学习与单元结算 7 项);原 1 项(V1.34.0 语音识别优化:埋点字段命名文档内不一致);后续出现版本冲突、需求口径或埋点 / 数据表缺口时继续收拢到此文件。

## 养库流程(新会话照这个做)
收到「版本号 + 飞书链接」后:
1. `python3 scripts/fetch.py "<链接>" <版本>` → 抓取+下图+版本,存 `raw/<标题>/`(超大图自动生成 `.read` 副本)。
2. 【Claude】读图:≤6 张全读;>6 张只读关键图(超大读 `*.read.png`);其余存档+链接。
3. 【Claude】判断模块(查 `taxonomy.md`:新功能→新建一级、已有→归二级,新模块写回)+ 提炼,落 `notes/日期-模块-标题.md`:
   - frontmatter:`标题/模块/版本/related: []/原链接/日期`
   - 正文:`## 摘要`(一句话)、`## 功能信息(逻辑/状态/边界/字段)`、`## 图说明`(超大图标注+本地链接)
4. `python3 scripts/relate.py --write` → 算双向 related(**同二级模块** 或 共享功能标识符;一级模块不计分,参数字段按命名模式剔除)。
5. `python3 scripts/build_index.py` → 重建 `INDEX.md`。

## 检索(写用例前)
`search.py 关键词` / `search.py --module 奖励` / `search.py --version V1.31`;模块历史/新需求关联+回归点见 `SKILL.md` 取流。

## 待办 backlog
1. ~~取③ 实测~~ **✅ 已验证(2026-06-30)**:拿「挑战课/Quiz改版」当模拟待测新需求,`search 关键词检索 → 命中 6 条相关往期线 → 深读 → 现算交叉回归点`,链路跑通、召回与排序合理。关键正面发现:**关联漏连被关键词检索完全兜底**(单词回顾/数据埋点 related 为空仍被检索拉出)。真实使用应丢库外新需求链接(模拟件会命中自己,检索时排除自身)。
2. ~~relate 优化(漏连)~~ **⚠️ 判断方向记反了,2026-08-28 已更正并修复**:当时基于 46 篇认为"漏连",扩到 102 篇 / 31 模块后实测是**过连** —— 426 对关联里 251 对(59%)纯粹来自"同属一个一级模块",AI Tutor 17 篇彼此全连、13 篇顶着 16 条 related,`related` 退化成"模块花名册"。已改为:**一级模块不计分,改用二级模块**(102 篇里只命中 18 对,全部是同一功能的历次迭代);标识符按**命名模式**剔除参数字段(`*_id/_name/_type/_count…`)替代手工黑名单(黑名单已失守三次:`level_number` → `image_count` → `block_type`)。结果:关联对 426 → 106,≥10 条关联的篇 42 → 2,零关联 6 → 35。**另证伪一条**:不能用 df 上限剔通用词 —— 清掉参数字段后 df 最高的是 `lesson_speech_recognition_end`(7 篇)、`app_enter_quiz`(6 篇),df 高恰恰说明该埋点被反复改动,是回归最该盯的信号。
3. **search 打分修正 ✅ 已完成(2026-08-28)**:改前 `search.py AB` 命中 63/102 篇、`id` 命中 68/102 篇,等于没筛。已改为:英文/数字关键词按**词边界**匹配(中文仍子串;用 `[a-z0-9_]` 前后视断言而非 `\b`,否则 `AB` 匹配不上「AB测试」)、正文命中按该词**库内覆盖率降权**(≤10% 记 1.0 / ≤30% 记 0.6 / ≤60% 记 0.3 / >60% 记 0.1)、排序改为**先看命中几个关键词再看分数**、默认只列前 15 条(`--all` 全量)。结果:`AB` 63→35、`id` 68→40,且高覆盖词(如「埋点」覆盖 75%)自动沉底;埋点事件名检索精度不变。
4. ~~打磨成正式 skill~~ **✅ 已完成(2026-07-01)**；**2026-07-07 合并为单一 skill**：撤销此前的薄入口，统一为 `qa-knowledge-base` 一个全局 skill（唯一入口，本体即 `/Users/dong/.codex/skills/qa-knowledge-base/`）。新会话自然语言调用 `qa-knowledge-base` 即可（写用例前查往期 / 把飞书需求入库）；本体规则、脚本和数据均在本目录。
5. ~~fetch.py 补内嵌 Sheet / Bitable 抓取~~ **✅ 已完成(2026-08-28)**:已固化进 `fetch.py`(见上"当前进度"✅ 条),产出 `raw/<slug>/sheets.json` + `meta.json` 的 `embedded_sheets`/`embedded_bitables` 计数,实测 80 张内嵌 Sheet 全部可读、体积较临时脚本降 37–88 倍且非空单元格零丢失。**剩余**:存量 20 篇未补抓 + 17 篇临时 schema 未统一,需一次回填。
6. **飞书 wiki 空间授权**:当前应用只能读单篇分享文档,无空间读权限(`131006`),所以只能手动一篇篇丢链接;授权后可批量。

## 关键约束(CLAUDE.md)
纯文件、不上数据库/向量、只收飞书需求文档、不直接生成测试用例(下一阶段)、原料层只读、脚本带详细日志、依赖项目级不全局装。
