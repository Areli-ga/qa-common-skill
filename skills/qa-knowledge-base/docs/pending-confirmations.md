# 待确认清单

> 只收需要主人、产品、研发或埋点表进一步确认的口径。普通测试重点、已明确本期不做、单纯风险提示不放这里。确认后同时更新本表和对应 `notes/`。

## 当前待确认

| 日期 | 笔记 | 待确认内容 |
|---|---|---|
| 2026-08-24 | [学习路径-路径双分支与搜索角标](../notes/2026-08-24-学习路径-路径双分支与搜索角标.md) | ① **「课程分支创建后可否修改」正文与设计稿直接冲突**：正文「编辑弹窗课程分支**可改**，Level ID 不变」vs 设计稿「分支决定编号命名空间与角标配色…**创建后不可修改**」。这条决定要不要测「已有内容换分支」整条路径（编号重排/名称联动/角标/已学进度/埋点 branch_code 全跟着变）。② **设计稿仍是旧命名 `Set n`**（修订记录 08-11 已定 `Phonics n`）；**角标配色**只在设计稿说明里出现，正文无定义。③ **`branch_number` 没有取值定义**（表里「值」列自我指代），而它是全量埋点改造的关键字段。④ **约 400 个事件统一加参数但无灰度/兼容方案**：存量与新数据如何区分、老版本不带 `branch_code` 时报表如何兜底、是否双写。⑤ **「所有带 Level_Number 的事件」这个圈定方式不可执行**：给的清单里明显含不带该参数的事件（AppPaused/app_page_view/auth_*/retention_d1 等），QA 无法凭文档确定应测范围。⑥ **搜索角标历史数据兼容**：老 Lv6–Lv9 迁到自然拼读后角标从 `Lv 6` 变 `Phonics 1`，收藏/历史/分享链接里的旧角标怎么办。⑦ `Phonics n` 多语言与角标空间（比 `Lv 10` 长很多）。⑧ 图 1 右上「**课程体系迁移**」入口正文完全没提，是否本期交付、与分支 Tab 什么关系。⑨ 空分支引导文案写死 `Phonics 1`，主课程分支为空时应提示 `Level 1`。⑩ 等级编号「插入后原编号后移」在双分支下对已在学用户定位、埋点历史、角标是否错位。⑪ 必测清单里 **`TappingWithNoReponse` 拼写少一个 s**（全量列表是 `TappingWithNoResponse`）。⑫ 无验收标准；搜索 iv 段（AI 关联内容）超时无兜底时限。 |
| 2026-08-24 | [语音识别-超时提前提交与分年龄阈值](../notes/2026-08-24-语音识别-超时提前提交与分年龄阈值.md) | ① **Δ₁/Δ₂ 数值未定**（「取决于 wheat 那边设计的算法，最好支持动态下发」），测试前必须先拿到当前生效值与下发方式 —— 与 V1.35《音量阈值设定》同款坑。② **「基线」指哪个值没写**：现有 `speech_star` 阈值是 40/70，「放行阈值」是通过判定线还是星级线、与 40/70 什么关系全无说明。③ **主动截停时机无具体值**（提前多少 ms），且必须与 V1.34《自适应跟读计时》的动态录音窗（6s+0.5s×(音节数−1)、封顶 10s）对齐，否则两套计时打架。④ **截停后兜底未定义**：孩子还在继续说怎么办、截停后仍未出分怎么办；「主动停止**一次**」之后就不管了吗。⑤ **年龄取值来源与生效时机**（Onboarding 选的还是设置里改的、改完是否立即生效、多孩子如何区分）。⑥ **T3 把「大龄」和「异常值」混一档**（「≥7 岁**或其他值（比如 -1）**」），年龄缺失的低龄孩子会被按最严档判。⑦ **影响范围未圈定**：课程跟读/跟读挑战课/故事书跟读/闪卡跟读/Mai 对话/破冰仪式都用语音识别，本期改哪些没写（破冰的 RMS 唤醒不应被误改）。⑧ **与 V1.36「ASR 打分切自研模型」的关系**：阈值基于哪个模型标定，两模型分数分布不同、Δ 不能通用。⑨ **全篇无埋点** —— 核心目标「低龄通过率接近高龄」无法验证，也没说用 `lesson_speech_recognition_end` 的哪些字段做分年龄对比。⑩ 无验收标准；无「改动后判定过松（读错也通过）」的反向风险控制。⑪ 文档模板残留（修订记录 `2026-mm-dd:`、主要特性空、资源列表全空）。 |
| 2026-08-24 | [新用户引导-Onboarding去掉选择语言](../notes/2026-08-24-新用户引导-Onboarding去掉选择语言.md) | ① **「对照组」截图缺失**（正文「对照组：」后为空）——「选择年龄的页面**修改回历史版本**」的目标形态无法从文档取证。② **下架范围不清（最关键）**：V1.36《Onboarding 流程引入中文》五项中，本篇只明确了「去掉选择语言」「保留菜单切换语言」，**「中文锁 Level 1」「中文首页直接是小 Mai」「每个孩子语言进度独立」是否一并下架完全没写**。③ **存量实验组用户如何迁移**：已选中文、已在中文路径学习的孩子，下架后走哪条路径、进度怎么处理 —— 回退类需求最易出事处，文档只字未提。④ **`learn_language` 用户属性去留**（V1.36 新增）及历史埋点归属。⑤ **「语言基于设置中的语言」与 V1.36「每个孩子语言进度独立」可能冲突**：设置里的语言是账号级还是孩子级、多孩子切换时如何取。⑥ **「切换学习语言」弹窗正文没描述**：图 2 有两态（带 Lv1–Lv2 进度条 / 带「中文课程内测中…」提示条），哪个是本期形态、进度条含义未定义。⑦ **无埋点说明**：`onboarding_language_click` / `add_child_language_click` 下架后是否停报、删除、报表断档如何处理（这两个事件在同批双分支篇的全量清单里都还在）。⑧ 文档极简（正文仅 223 字符）：无基本信息表、无 Owner/优先级/版本号字段、无背景与目标、**无验收标准**。 |
| 2026-08-24 | [同桌-邀请同桌1.2游戏挑战](../notes/2026-08-24-同桌-邀请同桌1.2游戏挑战.md) | ① **三套叫法并存**：埋点 5 个 `studybuddy_*` + 1 个 `deskmate_invite_share`；UI 上 `Invite a studybuddy` / `Enters it in Deskmate` / 中文「同桌」。② **原文自带两个待定**：双方 level 不一样时宝箱怎么算；开宝箱动画是否直接复用扭蛋动画。③ **宝箱 50 Giggles 归属歧义**（每人各 50 还是共 50）、双方是否都要各自点开、一方点开后另一方状态如何同步。④ **进度条口径两处不同**：组队页「当前 Level 进度（已学/总课程）」vs 首页入口「我和同桌中**学习最慢**的那个人的进度」。⑤ **微信/WhatsApp 分享文案缺失**（原文只有一句「微信分享卡片和 Whatsapp 的文案！」）。⑥ **背景暴露的最大卡点未解决**：漏斗显示「卡点：登录，**90% 用户流失**」，本期只改轮播与分享渠道。⑦ **5 个可挑战游戏全部依赖《游戏大厅 2.0》上线**，两篇提测顺序需绑定。⑧ **挑战对局机制完全未定义**：异步挑最高分还是实时？对方无最高分时能否发起？是否消耗 20 Giggles？结果是否计入榜单？⑨ 「最近在玩的 3 个游戏」取数与展示规则不全（不足 3 个、6h 边界、列表为空）。⑩ 「右上角展示同桌最佳成绩」本期做不做未拍板。⑪ **设计稿有正文没提的元素**：`Leave Team`（退出组队后宝箱/进度/奖励如何处理全无定义）、`14 days together` 与周打卡。⑫ 受邀者链路「本期不改」与新增 H5 落地页的衔接、邀请码有效期未确认。 |
| 2026-08-24 | [游戏模板配置-单词展示模式字段](../notes/2026-08-24-游戏模板配置-单词展示模式字段.md) | ① **命名三处不统一**：中文「仅图片/仅单词名/图文随机混用」vs 枚举 `IMAGE_ONLY`/`MIXED`/`TEXT_ONLY` vs 打包节点 `word_display` vs 组件名 `WordDisplayMode`。② **「图文随机混用」抽签算法写死端上、Admin 不可配**：`image_ratio` 是每主题还是全局？**QA 如何验证随机分布**（无入口、无埋点、无种子）—— 本篇最难测的点。③ `MIXED` 下「原画图片必填」与「部分显示单词名」初衷是否矛盾。④ `TEXT_ONLY` 切回 `IMAGE_ONLY`/`MIXED` 时历史缺图词卡会被 Generate 阻断，有无批量提示/缺图清单/定位跳转。⑤ 「同屏候选项必须同源，故不按角色分开配置」与背景「控制目标单词/干扰词/起始词」冲突，背景是否过时。⑥ `image_ratio` 全文只出现一次、无定义；「场景全量词库」只在图 1 出现、正文未列。⑦ Generate「缺图阻断」的提示形态未定义。⑧ `lemma` 快照必要性待与后端确认；lemma 与词卡显示名不一致时以哪个渲染。⑨ **不勾选时端上走包内默认值，但默认值是什么没写**（若非「仅图片」会改变全部存量课程表现）。⑩ Unity 侧标注「随课程制作」，本期端上渲染何时可测。⑪ 全篇无埋点无验收标准；文档模板残留（修订记录 `2026-mm-dd:`、主要特性空、资源列表全空）。 |
| 2026-08-24 | [语音识别-故事书跟读单元下移到句与结算页海报](../notes/2026-08-24-语音识别-故事书跟读单元下移到句与结算页海报.md) | ① **与同版本《故事书使用方式引导和 gigglecast 升级》直接冲突（最高优先级）**：本篇验收第 11 条「点推荐位直接进入下一本跟读模式，**不再二次选模式**」vs 那篇「每次打开故事书都唤起模式选择」「下一本书播放时进入模式选择」。② **音色与句的绑定关系未定义**（验收要求「同页两个音色各自播放自己的音频」，但一句是否可跨音色没写）。③ **录音存储 key 从页改句，存量录音如何兼容/迁移**（验收又要求存量单句页「零回归」、封面回放要能播旧录音）。④ 「可跟读句序列」一次性构建的稳定性（热更/内容变更是否重建、两次进入是否一致）。⑤ **口径表没有「已跟读句」**，但进度公式用它 —— 等于「已通过」还是「已推进」（有失败跳过时数值不同）。⑥ 「已推进句」「已尝试句」定义了却没说用在哪。⑦ **前导静音裁剪第二次悬空**（V1.35 写「可选可后置」，本篇写「建议提为 P1」仍未拍板）。⑧ 封面回放间隔「同页 0.2s/跨页 0.4s」只在对照表出现一次，无依据。⑨ 推荐位的「系列」数据来源、以及「不渲染且布局无空洞」的降级表现无设计稿。⑩ **海报二维码的隐私边界**：孩子录音要对外可访问，是否公开/有效期/撤回？V1.35 提的「完课结算页上传授权」前置是否具备。⑪ 分享文案多语言与可变性（面板有刷新按钮但只给一条英文）。⑫ 埋点细节不在本篇，`sentence_index` 起始值与 `sentence_id` 关系需去另一篇核对。 |
| 2026-08-24 | [游戏模板配置-路径新游戏模板与难度](../notes/2026-08-24-游戏模板配置-路径新游戏模板与难度.md) | ① **设计稿与规则直接冲突**：正文规定「点名连连看无难度配置、仅 3 个模板展示难度字段」，但图 1 已选模板正是**点名连连看**却显示了「难度=困难」下拉。② **枚举中英两套**：`easy/medium/hard/**expert**` ↔ 简单/中等/困难/**超高**（不是 extreme/super，易错）。③ **点名连连看无难度字段，却写「按档位连播 speakTimes 遍」「手动洗牌按档位限 2 次」** —— 档位从哪来，且「按档位又固定 2 次」自相矛盾。④ **难度参数非单调，疑似笔误**：托盘「每词在散落区几份」4→3→3→**4**；钓鱼「钩位数」8→6→**8**→7（困难比中等宽松）；跑酷「开局人数」6→11→11→**10**。⑤ 钓鱼「一竿容错次数」疑为派生值（钩位数−每竿带数），若派生不应单独配置。⑥ 「钩位数必须 ≥ 最长一竿带数，配置时自动抬」—— 是否回写配置/提示运营；与「命中核心区带数+1、钩位+1」「上限 6 带」叠加后边界需验证。⑦ 跑酷「每排门数」四档全是 3，但核心循环写「一排 2~4 扇门」。⑧ 保底通过率约束（不躲 60%/全躲 40%）是设计约束还是运行时校验，四档是否都满足未推演。⑨ **玩法参数章节复制粘贴错误**：跑酷段写着「每竿题目带数、一竿容错次数、核心宽度」，跑酷没有「竿」和「核心宽度」。⑩ 点名连连看每回合 18 格 vs 每词 4 块只能放 4 词（16 块），剩余 2 格如何处理。⑪ 难度「由模板 capability 驱动」，本期 capability 从哪读、机制是否已有。⑫ **与《游戏大厅 2.0》难度档位不一致**：大厅介绍页是 Easy/Medium/Hard **三档**，本篇路径侧是**四档**。⑬ `TappingWithNoResponse` 新增事件天然高频，**无频控/合并上报定义**，有数据量风险。⑭ `Number`/`RetryTime` 与既有 quiz 埋点是否兼容（存量 Challenge/Quiz 是否补）。 |
| 2026-08-24 | [消息推送-Push迭代推送规则与文案](../notes/2026-08-24-消息推送-Push迭代推送规则与文案.md) | ① **游客推送落地问题**：改「游客也推」但推送时间是「本地 19:00 **或自定义提醒时间**」—— 游客没登录、没设置中心，自定义时间怎么取？游客的「昨天学过/昨日推送类型/连胜天数」靠什么标识。② **反复弹授权弹窗无频控**（每天几次/上限/冷却全无），且与《任务体系》P3「打开消息通知」任务弹窗、`app_popup_push_show` 的 `scene` 枚举会不会连弹未定。③ **两套优先级编号冲突（必须先定）**：2.2 开头旧口径「P1 新关卡解锁>P2 连胜里程碑>P3 关卡进度>P4 单元进度>P5–P7 单元解锁节奏」vs 正学提醒新口径「P1 连胜>P2 小麦>P3 故事书>P4 Giggle TV>P5 课程类」。④ **新人引导与 19:00 全员推的互斥**：新人引导时点是 14:00/18:00/19:30，不是 19:00，如何判定「已命中」。⑤ **「昨日推送不是 X」在无推送日的取值未定义**（P1–P4 全依赖它）。⑥ **连胜倍数重叠未定义**：21 天（3&7）、30 天（3&10）走带奖励还是纯里程碑档 —— 直接影响能否真领到 Giggles。⑦ **P5 只有文案表、无档位判定规则**：44 条分 8 档，什么条件走哪档完全没写；「30 天内不重复」按条目还是按档位去重也未定；「单元主题跟进-practice」**只有 1 条**文案，疑似漏写。⑧ 旧规则「P1、P5 跳过数值为 0 的文案」在新表是否仍执行、`${lessons_left}=0` 怎么办。⑨ **14:00 印尼功能类 push 缺触发条件列**；「不相同功能不要两天连续推」无实现口径。⑩ **14:00 印尼推可能撞禁推表**：V1.35 明确要避开 TPA 古兰经班/祈祷时段，14:00 落在 Dhuhr–Asr 之间，必须交叉核对。⑪ **依赖未上线功能**：14:00 推的「游戏大厅」依赖 V1.37 游戏大厅、「PK 真人匹配」依赖真人数据、「邀请同桌」依赖 Study Buddy（单开需求），未上线时是否下掉未说明。⑫ **挽留召回与「每天 1 条」冲突**（day2 要 14:00+19:00 各一条），且 17 条召回文案含功能推荐，与 14:00 功能池高度重叠、无去重规则。⑬ 「其他活动」章节**表述截断**（只有「连胜、排行榜、」），与「不变」清单里的排行榜矛盾。⑭ **Push 链路埋点章节为空** —— 本期核心目标是提升触达率，却无下发/到达/点击/优先级命中分布/授权转化任何埋点（对比同版本 WA 篇有完整 `wa_push_send` 系列）。⑮ 变量写法不统一（`${streak_days}` vs `{N}`）；`${progress}` 格式未定义（80% 还是 4/5）。⑯ **本地化范围未说明**：文案全英文源，但「14:00 仅印尼语用户」必须有印尼语文案；Tolgee key 与未翻译兜底全无。⑰ 「本期会增加任务、小麦」但**任务 push 的优先级与文案没给**。⑱ 规则生效以「1.36 先上的技术优化」为前提，需确认 1.36 是否已发。 |
| 2026-08-24 | [竞技场-游戏大厅2.0小游戏](../notes/2026-08-24-竞技场-游戏大厅2.0小游戏.md) | ① **游戏数量口径不一**：介绍页写「所有 **9 个**游戏统一 20 Giggles/次」，正文只列 6 单人 + 2 多人 = 8 个，排行依据表只 6 行。② **同一游戏两个名字**：正文「单词 PK 无尽模式」vs 排行依据表「单词谁记得多」（影响榜单标题与埋点枚举）。③ **排行榜单位设计稿与规则表冲突**：规则表单词PK 单位「胜」，全国榜设计稿显示 `27 Lessons/18 Lessons/15 score`、全球榜显示 `Scores`，三种单位并存。④ **本篇无埋点方案**，只有 3 个开发提问 + 外部 `mini-game-tracking-review.md`；提问本身已点出硬缺口：**分享按钮无埋点**、**单机游戏三入口（课程路径/大厅/分享外链）不区分来源**，不解决无法评估「分享驱动传播」。⑤ **强前置未闭环**：入口改造依赖 `homepage_toplearners` **AB 实验组全量**（Deskmate 回位 / 排行榜移位 / Game Arcade 替换家庭游戏），需确认全量时间点与本期上线先后。⑥ 「替换掉家庭游戏入口」= 家庭游戏下线还是移位未说明。⑦ **6 个游戏的判定/计分/失败条件全在外部 PRD md**（PRD_点名连连看.md 等），本文档只有一句话玩法 —— 写用例前必须另取。⑧ **20 Giggles 扣费时机与兜底全缺**（进入前扣/结束扣、余额不足提示、中途退出退款、多人由谁扣）。⑨ 钓鱼钩卡单位「米」与「胜」「分」不同量纲，榜单 UI 复用需确认。⑩ 百科常识 1000 题：本地化语言范围、Admin 配置「可能需要」、**答完 1000 题后的行为**未定。⑪ 抢答游戏本地多人的「赢」判定与按键分配不在本文档。⑫ **免登录试玩数据归因未定**（游客成绩是否入榜、如何与安装后账号打通、防刷）。⑬ 两处「待定：等 bolin 设计完第一个再敲定」（浏览器外横屏提示、结束页），本期是否交付未定。⑭ 分享 title/description 5 组未定稿且全部写死「Listen & Match」，**其余 5 个单人游戏分享文案未给**。⑮ Study Buddy / 邀请同桌 1.2 是单开需求，但核心目的第 2 条依赖它，本期社交闭环能否成立待确认。 |
| 2026-08-24 | [Giggle TV-童锁与自动播放](../notes/2026-08-24-Giggle%20TV-童锁与自动播放.md) | ① **章节与标题不符**：标题写「改名 + **自动播放**」，正文小节只有「本期：童锁」「下一期：投屏」，自动播放塞在童锁小节里 —— 是否本期交付需确认。② **埋点跨期混装 + 有正文未覆盖的功能**：`giggletv_cast_*` 属下一期投屏；**`giggletv_tab_click` 的「推荐/专辑 tab」正文完全没有**（正文写「进入后主页不变」，设计稿是 Nursery Rhymes tab），「算法推荐」这条内容分发方式在本文档无任何需求描述。③ **童锁锁定范围未定义**（返回键/进度条/系统手势是否锁、锁定期间能否退出 App、会不会被 5s 自动播放切走）—— 童锁核心价值就是防误触。④ **童锁持久化未定义**（切视频/退出重进/杀进程）。⑤ **自动播放 × 屏幕时间 × 童锁 三者交互未定义**（Screen time 到点会 pause all games and stories，列表循环是否受限）。⑥ **循环逻辑是否与 GiggleCast 共用**：同版本故事书篇给 GiggleCast 定了 `single`/`list`（`gigglecast_loop_mode`），本篇是另一套「整列表循环 + 不足 5 个取最早补足」。⑦ **「投屏使用率」测不准**：走系统自带投屏，App 只能埋「点了按钮」，是否投屏成功/时长拿不到。⑧ 投屏教程 6 品牌文案未定稿，且**未覆盖机型无兜底**（Infinix/Tecno/Realme/一加等印尼常见品牌不在列）。⑨ **改名影响面需一并回归**：Songs→Giggle TV 牵动 V1.36 歌曲离线下载与 Downloads、WA 深链 `paramValue=Songs`、功能广场配置与 `tab_name` 枚举、Tolgee 多语言文案。⑩ 埋点「新增日期」是未转换的 Excel 序列值 **46230**（≈2026-07-27）；缺「专辑播放」维度字段。⑪ 页面标题仍是 `Songs`（设计稿），与新入口名 Giggle TV 不一致。⑫ 无验收标准；无视频加载失败/弱网/切后台/投屏中断兜底。 |
| 2026-08-24 | [AI Tutor-新用户唤醒仪式优化](../notes/2026-08-24-AI%20Tutor-新用户唤醒仪式优化.md) | ① **全篇无埋点**：唯一目标「通过率 65%→85%」只能靠 V1.35 的 `ritual_start`/`ritual_woke`（其 `mai_` 前缀之争至今未决），本期新增的 Combo 连击、循环文案进度、ASR「Hi」兜底触发全部无法量化。② **阈值口径不清**：「达到阈值、没有达到 0.1S」是新增时长门槛还是沿用旧值？与破冰篇 RMS 0.15–0.18、音量阈值篇 Speech 15 三种量纲需对齐。③ **ASR 兜底「Hi ×3」细节全缺**：累计范围/超时清零、近似音（hai/hey/嗨）是否算、用哪个引擎（V1.36 已切自研）、**是否计电量**。④ 两个 iOS 问题只解了一个 —— 「麦克风指示灯亮但音量进度条无反应」（已验证 BUG）本期是否修没写。⑤ 狂点时动画/音效队列策略未定义（可连触发动画 vs 不可连触发语音提示），Combo 计数与断连规则也无。⑥ 「模仿出『起床啦』的效果」原文写的是「需要调研」，本期是否交付未定。⑦ 闭麦播提示与「大声喊」冲突，无提前收音/视觉信号。⑧ 「右下角入口变为动态睡觉方案」仅一句、**本篇无图**。⑨ 文档版本号字段为「（待填）」、修订记录为空；无验收标准。 |
| 2026-08-24 | [AI Tutor-作息陪伴与冻结解救](../notes/2026-08-24-AI%20Tutor-作息陪伴与冻结解救.md) | ① **8.7 章节整体缺失（最严重）**：全文 6 处引用 8.7（**道别编排三种方式、上下文字段落库清单、Prompt 铁律与话术冻结策略**），第八章只到 8.6 —— 而承接矩阵 P3/P4 判定完全依赖 `last_exit` 字段。② **数据埋点章节为空**（只有「埋点和参数参见：」）：主指标（prayer_based 次日/7日留存 +10pp、日均会话 +3pp）、P1–P7 命中分布、解救 combo 完成率、约定兑现率全部无法量化。③ 需求范围第 10 条（**Unity banner：写死入口/动画/scene id/AB test**）在第八章无对应小节，AB key 未给。④ 目标「+10 个/3 个百分点」未说明哪个数对应哪个指标。⑤ **两篇唤醒规则矛盾**：本篇轻唤醒「点击 combo 十下即可唤醒」vs《新用户唤醒仪式优化》「点击不允许达到阈值」。⑥ **冻结起点三处不一**：第七章「连续第二天不活跃」/ 8.4「渐冻期第 1–6 天」/ P1「freeze_days>=1」。⑦ **礼拜时刻「零容忍」与城市级 IP 定位冲突**：跨城/VPN 会直接算错，却无手动选城市/纠错入口与偏差容忍度。⑧ **合规边界**：字段不含宗教词，但冻结词表本身是宗教词表且判定依赖对话内容（经 ASR/LLM 与日志），需法务确认。⑨ 探测覆盖不对称：系统探测仅印尼语，主动打标适用所有语言。⑩ `rhythm_mode` 主动切回后 `probe_status` 取值与频控未定义。⑪ 交付标准写 C1–C12，用例清单到 C14（C13「约定覆盖」正是单槽规则唯一验证用例）。⑫ `calendar_overlay` v1 不实现，**斋月期间作息大变**无临时方案。⑬ **本篇无图**，Living Sky/渐冻 7 日序列/冰雕锤子/转场动画全在外部 demo 与 Figma，文档内无法取证。 |
| 2026-08-24 | [AI Tutor-MtownTalk小麦多轮指导与镜头切换预研](../notes/2026-08-24-AI%20Tutor-MtownTalk小麦多轮指导与镜头切换预研.md) | ① **全篇无埋点**（同模块 V1.36 bbox 篇亦无，连续两版缺失）：目标「使用时长 +100%」、三种激活方式触发量、胶囊拒读率、object 点击、动态 prompt 分级结果均无法观测。② 「**连续两轮开麦但没有推进剧情**」判定标准未定义（无识别结果/静音超时/语义不相关各算不算）。③ 小麦激活「最高优先级」的**抢占规则**未定义（NPC 正说话、用户正录音、音效正播时如何处理）。④ 胶囊关闭按钮**仅限中英文**，印尼语（主力市场 80–90% 用户）无此能力，是否有意。⑤ **电量口径仍空白**：本期新增「同时触发多个 object」+ 小麦多轮反问，与 V1.36 电量系统（子游戏准入 ≥0.01 美金、余额不可为负）的关系、连点频控均未定义。⑥ 「音效播完才进入下一轮对话」是**暂定**；多 object 同触发时等第一个还是最后一个、长音效是否卡死对话无超时兜底。⑦ **动态 prompt 分级依据与落库全缺**（判定信号、是否持久化、跨场景/跨课继承、是否走 AB）—— 本篇最不可测部分。⑧ **「小麦睡着」三篇定义不一**：本篇「睡眠改监听态」/《作息陪伴》awake·asleep 状态机 + 冻结皮肤 /《唤醒仪式》破冰睡着态，是否同一字段、M Town 内小麦是否受作息影响需统一。⑨ 5.2 明确本期不开发，但「运营准备课件/制作 demo」「UI/UX 转场设计」是否属本期交付未说明。⑩ 版本号字段为「（待填）」；无验收标准与异常兜底（动画/音效资源缺失）。 |
| 2026-08-24 | [反馈与社区-WA推送体系接入](../notes/2026-08-24-反馈与社区-WA推送体系接入.md) | ① **首轮第 2/3 条消息链接与文案串位**（【群链接】配 Channel 文案、【channel链接】配群文案）。② **推送轮次表不对称**：day3 只有「有完课」分支、day4 只有「无完课」分支，疑漏两格；day4 之后是否停推未写。③ **WA 24 小时客服窗口**约束完全未提 —— 18h 节奏与 24h 会话窗如何配合（超窗须用模板消息且需审核），这是能否发出去的硬前提。④ **BSP Webhook 本期是否接**：`send_result` 只记同步返回，送达/已读（漏斗第 ⑤ 层）必须靠 Webhook。⑤ **深链缺口处置**：`unity/customsendmessage` 系 6 个入口只落主路径、练习册 QA 白屏，哪些本期必修需拍板（原文对专题课/自然拼读/歌曲写「尽量支持」、对口语写「可以暂时不处理」）。⑥ 家长中心学习报告 **QA 与正式目标页不同**（`learned-lessons` vs `weekly-learning-report`），与 V1.35 同一遗留。⑦ **家长验证口径冲突**：游客联系我们要加家长验证，WA 报告链接却「不需要家长验证即可查看」。⑧ 打标签只有 4 字段 + 「手动筛选」，标签体系/同步时机/失败处理全无。⑨ Admin 新增枚举链接写成占位符 `https://whatsappbusiness`。⑩ **闪卡两环境用了不同 scheme**（QA `h5/flashcard` 直达 vs 正式 `customsendmessage` 落主路径），是否统一为 h5 方案。 |
| 2026-08-24 | [故事书-阅读模式选择与播放器设置](../notes/2026-08-24-故事书-阅读模式选择与播放器设置.md) | ① **母语版/中文书归属不清**：08-05 已把 Native Reading 移出播放模式，核心表也没有该行，但附录仍写它「提供自动手动翻页 + 磨耳朵」，且设计稿画了「中文书」形态 —— 现在看中文文本走哪条路径未定义。② **语音克隆（voice clone）** 写进需求范围但正文无任何功能描述。③ **Ending Page** 只有三句行为，无页面结构与与结算页关系。④ **磨耳朵入口范围冲突**：正文只有原声模式有，附录写「原声播放、故事讲解及母语版」都有。⑤ **Creator 翻译开关口径自相矛盾**：双语字幕「不受该开关影响」，GiggleCast 声音切换却要求「creator 翻译开关为开启」。⑥ 录音模式「退出后自动进入原声播放模式」（附录）正文未重申，是否仍成立。⑦ 用户反馈提到的「中文字号小 / 双语 vs 全英切换」本期只做了开关，是否覆盖未说明。⑧ **与同版本《任务体系》冲突**：任务引导要求「打开故事书后引导跟读模式」，本篇要求「每次打开都弹模式选择」，谁先谁后、会不会双弹窗未定。⑨ 文档「版本号」字段原文为「（待填，来源于产品规划迭代 Roadmap）」，本库按主人给定 V1.37.0。 |
| 2026-08-24 | [学习中心-单元卡片介绍与标签配置](../notes/2026-08-24-学习中心-单元卡片介绍与标签配置.md) | ① **标签表缺 LV9**（需求范围写 L2～L9，表只到 LV8）。② 标签表**无表头**，两列（主题 vs 标签）哪列写进 `unit tags` 需确认；标签全英文，是否要本地化未说明。③ `unit tags` 为自由文本逗号分隔，**无枚举校验/数量上限**，存量单元标签如何回填未说明。④ Lv1 是否也走新介绍卡（原文写 Lv2 起，示例图却用 Unit 1）。⑤ **全篇无埋点**（介绍卡曝光、核心单词音频点击都没有）。⑥ 核心单词取值是否与《单元内连续学习》的 `unit_goal` 同源。 |
| 2026-08-24 | [反馈与社区-App内调研问卷](../notes/2026-08-24-反馈与社区-App内调研问卷.md) | ① **两套方案并存、范围未拍板（最高优先级）**：需求范围是「Google 表单 + 短链 + 按 uid 名单手动发 100 Giggles」，功能描述/原型/后台却是「自研问卷 H5 + 自动发 N Giggles + 后端返回答题状态」，两者测试范围几乎无交集。② **奖励数额冲突**：100 Giggles（需求范围）vs N 后台配置·示例 30（原型）。③ **游客奖励领取矛盾**：流程图写「游客登录后领取」，完成页却与登录态一致显示「您已获得 N Giggles」；游客「已完成」状态如何绑定（设备/游客 id）未说明。④ **埋点严重不足**：只有 `h5_survey_enter` / `h5_survey_submit_success` 两个事件、一个 `survey_name` 参数，核心 KPI「曝光→完成 ≥10%」算不出来，放弃率与四维拆分全缺。⑤ 「每月新增广告」vs「每两周一期」周期不一致。⑥ **定向条件无定义**（维度、配置位置、归谁管都没写）。⑦ 多语言链路口径不一：Step2 写「发布后同步 Tolgee 翻译」，流程图写创建时就填各语言译文；发布后翻译未完成时 App 展示什么未定义。⑧ 录音题细节缺失（麦克风权限拒绝、上传失败重试、导出音频命名与存储）。⑨ 已发布/已有作答的问卷能否删除、编辑是否影响已回收数据未定义。⑩ 新广告位与家长中心既有 banner（V1.35 WA 导流、V1.37 任务体系邀请海报）的**轮播位序与优先级**未协商。 |
| 2026-08-24 | [挑战课与Quiz-单元测验线索提示类型渲染](../notes/2026-08-24-挑战课与Quiz-单元测验线索提示类型渲染.md) | ① **「线索词是否为字母」判定依据未定义**（类型字段？长度=1？白名单？），**多字母组合如 `sh`/`th` 算不算字母**直接影响自然拼读题。② **未启用题目不迁移** → 后续被启用时 Clue 仍是旧值，会重新暴露字母；启用动作是否加校验未说明。③ **新建题目的默认 Clue 类型未定义**，后台新增字母题若默认 Text+Audio 会重现同一 bug。④ 提示音频缺失的拦截口径：整批阻塞还是跳过该题？跳过的题最终 Clue 是什么。⑤ Audio 的「进入自动播一次」与横版闯关的背景音/跳跃音效是否互相打断未说明。⑥ **全篇无埋点** —— 「恢复真实考核效果」的验证指标（答对率变化）无法量化。⑦ Image / Image+Audio 端上未实现，**后台是否要禁止选这两个类型**未说明，否则运营仍可配出无提示的题。 |
| 2026-08-24 | [任务体系-任务面板与任务列表](../notes/2026-08-24-任务体系-任务面板与任务列表.md) | ① **任务优先级缺 P5**（P1–P4 后直接跳 P6），是否漏配一个任务。② **老用户任务数量**：指标表「2 任务 + 邀请兜底」vs 详情「老用户有三个任务」。③ **小麦命名四套并存**：埋点 `task_wheat`/`wheat_feature_*` vs 引导埋点 `task_mai_intro1` vs 文案表「唤醒小麦AI / Wake Up Xiaomai AI」vs 设计稿「叫小麦起床 / Go wake up Mai」，不统一会让埋点分析对不上。④ **卡片文案与三语表不一致**：三语表 P4「跟读一本故事书」vs 设计稿「读一本书给妈妈听」；P2 同理。⑤ **P2 奖励未定**（一套服装「或者特殊的头饰」），全部奖励商品待 Alice 设计。⑥ `task_status` 的 `gifted`（中文「领取礼物」）与 `task_reward_claim` 的先后关系未定义。⑦ **消息通知任务收尾路径两处不同**：任务表「回到任务列表页面」vs 详情「首页中自动打开任务面板页面」。⑧ **任务面板与其他弹窗优先级未定**：老用户「单元勋章发放后主动弹面板」与同版本《单元内连续学习》的「教学体系更新弹窗优先级最高」及既有首页弹窗体系如何排序。⑨ 新用户实验组「删除原本的 Giggles 购买弹窗引导」是仅实验组删还是全量下线（需与 V1.34 路径奖励 AB 对齐）。⑩ AB 分流按「游客 id 生成时间 8 月 21 日」，重装/换设备后游客 id 变化如何归组未说明。⑪ 小麦引导两个方向（用户主动点击 vs 系统自动点击）本期做哪个/并存未定。⑫ **无验收标准与异常兜底**（奖励发放失败、领奖动画中途退出、面板加载失败）。 |
| 2026-08-24 | [学习路径-单元内连续学习与单元结算](../notes/2026-08-24-学习路径-单元内连续学习与单元结算.md) | ① **课程介绍页与实际路径的自然拼读口径不一致**：设计稿是 `阅读启蒙` 5 个 Set（各 7 单元、25–30 CVC 词），学习中心自然拼读 tab 是 `A1 · 阅读准备` Phonics 1–4（9/8/8/8 单元，7/64/64/64 单词），Set 数、命名、单词数三处都对不上。② **闪卡跟读是否计入本期开口次数**：线上无配置，原文写「本期来不及可以后面提需求加」，范围未定。③ **新主线 L6–L9 奖杯「待配置」**：配置未就位时，完成升级 Quiz 的用户拿不到奖杯，降级表现未说明。④ **单词卡内 L6–L9 单词处理方案**原文即未决；且「自然拼读阶段隐藏单词卡」与「单词卡按 Lv1–Lv9 分 tab」如何并存未说明。⑤ `unit_summary_nextstep` **触发点描述为空**；`unit_barge_*` 疑为 `unit_badge_*` 拼写错误（原文两处均写 barge）。⑥ **升级 Quiz 题目混杂其他等级**根因未定（level up 包问题 vs 兜底逻辑取错类型）。⑦ 教研大纲文案未确认；中间过渡页「是否提前加载下一节课」本期是否做未定。 |
| 2026-08-11 | [AI Tutor-Mai电量系统成本控制](../notes/2026-08-11-AI%20Tutor-Mai电量系统成本控制.md) | ① **正文残留旧口径**:6.1 范围内仍写「余额可负(UI 显示 0)」,与 07-22 修订及 8.1「**余额不可为负**」矛盾。② **发布策略两处不一致**:6.1 写「拥有小麦权限的用户 100% 开放」,8.4 写「所有灰度名单内的用户 100% 开放」。③ **免费池重置时区待定**(建议用户本地时区 vs 统一 UTC+8)。④ 设计稿按钮仍是 `Exchange`,正文要求改 `Recharge ⚡`。⑤ **无兑换失败/达上限/准入拦截的埋点**,被门槛挡住的用户量无法量化。 |
| 2026-08-11 | [语音识别-变声器与朗读录音卡片升级](../notes/2026-08-11-语音识别-变声器与朗读录音卡片升级.md) | ① **第 6 个滤镜名称三处不一致**:正文「大山谷 🏔️」、埋点 enum `valley`、设计稿同时出现 `Echo` 与 `Cow`。② 验收标准提到「子游戏内滤镜按**随机策略**出现」,但正文**未定义该随机策略**(概率/频次/点位)。③ 验收写「录音**红点**提示」,功能描述写「**波形**提示」。④ `mai_app_update_click` 备注写 **1.37.0**,与本篇 V1.36.0 不符,升级提示到底哪版上。⑤ 变声鼓励重复开口/重录,**是否计入电量**未提(与同版本电量系统冲突)。 |
| 2026-08-11 | [AI Tutor-小麦中文课程](../notes/2026-08-11-AI%20Tutor-小麦中文课程.md) | ① **中文不启用打分、改用 ASR 文本匹配** —— 跟读反馈/鼓励文案/依赖星级的下游逻辑(如开口激励的"判定通过")在中文课下如何取值需确认。② `learn_language` 是**用户属性而非事件属性**,切换语言后历史事件归属未说明。③ **灰度规则只列了语言维度**,无比例/名单/开关口径。④ 中英文切换入口的位置、"正在学习语言"标识的形态**文档内无界面描述**。 |
| 2026-08-11 | [AI Tutor-MtownTalk支持bbox点击](../notes/2026-08-11-AI%20Tutor-MtownTalk支持bbox点击.md) | ① **餐厅场景文案从商店直接复制未改写**(兜底话里写「这里是**商店**」,"用户服侍"两条与商店逐字相同)。② 示范句式语法不严谨(`My {部位} hurt.`、`Can I have {对象}, please?`),是否修正需产品定。③ **每次点击触发一轮 AI 回复即消耗电量**,是否计费/有无频控未说明。④ 未定义:连续快速点击、点击空白区域、AI 回复超时的兜底。⑤ **全篇无埋点**。 |
| 2026-08-11 | [语音识别-语音输入与朗读组件一致性设计](../notes/2026-08-11-语音识别-语音输入与朗读组件一致性设计.md) | ① **「单词染色接入」只有六个字 + 一张示意图**:染色粒度(音素/音节/字母)、颜色与分数档位的映射、生效场景、中文课染色方式全部未定义。② TTS 时间戳**两种技术方案未定**(时间计算平均 vs 接入千问官方时间戳),后者与 TTS 供应商切换强绑定。③ 自由麦「红块跳动」的音量阈值取哪一档未说明。④ **「不再支持字幕实时高亮」是能力回退**,需确认是有意为之。⑤ 无埋点、无验收标准。 |
| 2026-08-11 | [竞技场-PK接真人数据](../notes/2026-08-11-竞技场-PK接真人数据.md) | ① **「三、前端处理配合」只有一句「中途暂停,要开始计时」**,谁暂停、从何时计时、与对手回放时间轴如何对齐均未说明。② 机器人规则的**硬性约束**(≥1800ms、6 题用时不得全等、L1 防碾压兜底)对**真人回放数据是否同样适用** —— 若 L1 兜底生效等于篡改真人数据。③ 若 1.36 改了题数/计时/计分,`rule_version` 会让 1.35 采集的数据整体失效,**本需求是否还成立**需确认。 |
| 2026-08-11 | [新用户引导-Onboarding流程引入中文](../notes/2026-08-11-新用户引导-Onboarding流程引入中文.md) | ① **中文级别页设计稿箭头指向 L2**,与正文「中文无论多少岁都锁 Level 1」矛盾(埋点表称其为"中文 Level1 锁定页",支持锁 L1)。② 中文首页流程原文注明「**以 Libin 需求为准**」,本篇不是最终口径。③ **未给出 AB key**,而本版已有多个首页级 AB 并行,需补 key 并确认分层。 |
| 2026-08-11 | [奖励系统-课程开口激励](../notes/2026-08-11-奖励系统-课程开口激励.md) | ① **门槛归属歧义**:门槛序列把轮次与课程绑定(第1轮 Hello N=3、第2轮 Color N=4),但进度是**跨课程累计**的——若第 1 轮在 Color 课攒满,这轮算 N=3 还是 N=4?② **"跟读判定通过"是几星**未定义。③ **同一次完课进度远超 N 时**多余次数顺延还是丢弃未说明。④ **全篇无数据埋点**。⑤ V1.34 实验组刚"去掉扭蛋",本期又在开口场景引入扭蛋,**两套奖励的共存/互斥关系**需确认。 |
| 2026-08-11 | [课程加载-离线模式V136](../notes/2026-08-11-课程加载-离线模式V136.md) | ① **儿歌空状态文案与 Figma 原文不一致**(正文 `Download nursery songs to listen offline.` vs Figma `Download songs to sing along offline.`,原文已自行标注)。② `tab_name` 枚举在不同事件间不统一(`story` vs `storybook`、资源管理类缺 flashcard、delete 多一个 `all`)。③ **挑战课本期"暂时不支持"离线,但 `ChallengeLessonStart` 也要求带 `linemode`**,offline 值是否会出现。④ 弱网 H5 的 7 个慢加载问题只给了方向("展示网络状况"),**无具体兜底样式与超时阈值**。⑤ 评审补充第 3 条「在【多】等子页面中无网络,点击下一本,无网络」**表述不完整**。 |
| 2026-08-11 | [排行榜-连胜打卡榜](../notes/2026-08-11-排行榜-连胜打卡榜.md) | ① **设计稿百分比与档位表对不上**:img-04「52 天 → 98%」(表应 90%)、img-05「2 天 → 80%」(表应 38%),疑为占位数据。② **榜单更新口径矛盾**:页面文案「每天凌晨更新数据」vs 规则「用户完成学习后再进入,实时刷新排名」。③ 命名未统一:正文「连续打卡榜」vs 埋点 `tab_name` 值「连胜榜」,且用中文枚举。④ `leaderboard_page_view` 的触发位置列被误填成图片 URL。 |
| 2026-08-11 | [语音识别-故事书跟读组件优化](../notes/2026-08-11-语音识别-故事书跟读组件优化.md) | ① **需求编号错乱**:正文只有需求一~六和需求九,**缺七、八**,且需求四里引用的「需求七(完课结算页回放/分享)」实际是需求五。② **需求四(剪前导静音)标注为"可选、可后置"**,需确认本期做不做——它同时影响单页回放与结算页连播。③ **分享"也可以不改"**,本期是否交付未定;且分享依赖「完课结算页上传授权」这一前置是否已具备。④ 弱网/短句路由自研模型的**阈值由 wheat 调参、不写死**,测试前需拿到当前生效值。 |
| 2026-08-11 | [AI Tutor-小Mai新用户入场破冰仪式](../notes/2026-08-11-AI%20Tutor-小Mai新用户入场破冰仪式.md) | ① **过渡到麦克风态需要点几次**:状态机与功能描述写「点击 **≥1 次**」、埋点说明写「点击后自动转场」,但**验收标准写「点击 ≥3 次自动过渡」**。② **埋点命名**:修订记录(07-22)要求「删除 2 个埋点、重命名加 `mai_` 前缀」,但正文表格仍是 `ritual_start` / `ritual_first_tap` / `ritual_woke` 无前缀旧名,且删的是哪 2 个无从对照,需以《Mai - AI Tutor 埋点》为准。③ **唤醒首句文案**:正文「嗨,我是小Mai,是谁把我吵醒了呀?…」vs 设计稿「是谁把我吵醒了呀?我是小Mai,…我们可以做好朋友。」 |
| 2026-08-11 | [AI Tutor-Mai基于全局单词本生成Quiz](../notes/2026-08-11-AI%20Tutor-Mai基于全局单词本生成Quiz.md) | ① **触发条件疑似笔误**:「且用户当天**打开过** Quiz(一套 Quiz 一天只触发一次)」——按语义应为「当天**未**打开过」。② **结算页与设计稿冲突**:正文说未满分有「Try Again」按钮、满分有「卷轴奖励」,设计稿(img-02)两态**都只有 Close 按钮、无卷轴奖励**,埋点也只定义结算页点 Close。③ **3 次机会用尽后**是「直到答对」还是「用尽即进下一题」,流程表与验收标准写法不一致。④ **文档版本号字段与 Figma 均为 1.34.0**,按主人口径入 V1.35.0,是否为延后需求待确认。 |
| 2026-08-11 | [语音识别-音量阈值设定](../notes/2026-08-11-语音识别-音量阈值设定.md) | ① **文首结论「取 70%」与正文建议值对不上**:正文给的是 SDK 绝对刻度 Noise 8 / Speech 15 / Reading 25–35 / Very Loud 70,「70%」指哪个量、用在哪一步未说明。② 正文末尾「推荐把参数设计成两层」**内容截断未写完**。③ 阈值是基于**微软模型还是自研模型**标定的(同版本正在切 ASR 打分模型)、是否在**降噪增益开启态**下标定,均需确认。 |
| 2026-08-11 | [AI Tutor-Mtowntalk场景对话课流程优化](../notes/2026-08-11-AI%20Tutor-Mtowntalk场景对话课流程优化.md) | ① 原文自带待确认:**「重玩默认进练习视角」**未定。② **埋点三种命名风格并存**(`MaiRoleSelectEnter` / `mai_role_start_chat` / `RoleSelectExit`),是否统一加 `mai_` 前缀需与破冰仪式篇一起对齐。③ **关卡选择页无 UI 稿**:角色卡的完成状态呈现、最近一次选择的默认高亮视觉均无图。 |
| 2026-08-11 | [AI Tutor-小Mai模型切换配置](../notes/2026-08-11-AI%20Tutor-小Mai模型切换配置.md) | 全文仅 506 字符,**分桶规则、配置入口与生效时机、切换失败的降级容错、自研打分与微软打分的口径可比性、「对印尼语分桶」的确切含义**均未定义;`tts_provider` 只覆盖 TTS,ASR 供应商无埋点字段。 |
| 2026-08-11 | [竞技场-PK机器人规则](../notes/2026-08-11-竞技场-PK机器人规则.md) | **杀进程/断网是否扣 PK 次数,两篇口径相反**:主 PRD《竞技场 1.0》写「进入后台或强制杀 App 按中途退出处理,**扣除 PK 次数**」;本篇边界表写「断网/杀进程 **不计次数**」。另:主 PRD 对「挽留弹窗点 Exit」只说数据作废、不计总场次,**未明说是否扣次数**(本篇写中途退出计一次)。 |
| 2026-08-11 | [奖励中心-家园好友串门社交测试](../notes/2026-08-11-奖励中心-家园好友串门社交测试.md) | **假数据更新周期冲突**:正文两处写「**24 小时**更新数据 / 24 小时刷新一批用户(每批随机加 3–5 人)」,设计稿 img-02 注明「刚开始测试使用假数据时,**48h 更新一次数据**」。 |
| 2026-08-11 | [学习路径-老用户路径迁移](../notes/2026-08-11-学习路径-老用户路径迁移.md) | **保留路径到底几条**:正文两处写「收敛为 ~~3~~ 2 条」「除 ~~3~~ 2 条保留路径」(带修改痕迹),但括号内列了 **3 个路径 ID**(819564819857413 / 810179729922629 / 823064397629317)。另「旧路径清理前置条件(全部满足才可执行)」只列了 1 条,其余条件缺失。 |
| 2026-08-11 | [反馈与社区-whatsapp加好友发送消息链路](../notes/2026-08-11-反馈与社区-whatsapp加好友发送消息链路.md) | ① 原文自带两处待确认:「配置了 WA 群链接后是否支持点击跳转?」「能否只删除印尼的文案?」② **家长中心学习报告的 Scheme,QA 与正式 H5 路径不同**(`#/learned-lessons` vs `#/weekly-learning-report`),需确认哪个是目标页。③ 走 `unity/customsendmessage` 的 5 个入口(专题课/自然拼读/绘画/歌曲/口语)+ 正式环境闪卡**均只能落到主路径**,哪些本期必须修复、哪些放下个迭代需产品拍板。 |
| 2026-08-11 | [排行榜-排行榜细节优化](../notes/2026-08-11-排行榜-排行榜细节优化.md) | ① **第 1 名动效顶部文案**:正文要求改为 `You're on top!`,设计稿(img-01)仍是 `Got to No.1`。② **冲刺气泡文案**:正文「再 X 关冲回前 10,拿回 100 Giggles!」vs 设计稿「再 X 关冲到前 10,拿下一等奖励!」。③ 气泡在**同分**(X/Y/Z 差值 ≤ 0)时如何取值,原文未定义。 |
| 2026-08-11 | [学习路径-解锁规则和学习顺序测试](../notes/2026-08-11-学习路径-解锁规则和学习顺序测试.md) | ① **Hello 课归属**:文中并存「把 Hello 当成单独的单元处理」与「第一单元内的课程处理」,影响第一个单元勋章的达成条件。② **解锁触发点**:实验组只写「完成已解锁的最后一个单元(第 2 个)才再解锁两个单元」,未说明只完成第 1 个单元时是否解锁。 |
| 2026-08-11 | [奖励中心-奖励中心优化需求](../notes/2026-08-11-奖励中心-奖励中心优化需求.md) | 埋点 `reward_cabintab_switched` **属性列为空**,无法区分切换到哪个家具主题 tab;需产品/埋点确认是否补 tab 名或主题 ID。 |
| 2026-08-11 | [消息推送-新用户push优化](../notes/2026-08-11-消息推送-新用户push优化.md) | ① **D0/D1 的 AB 组条数**:正文写 D0 = 14:00/18:00 两组、D1 = 8:00/14:00 两组,但两张文案表都各有第三条 19:30 —— 19:30 是 AB 之外全量再推,还是三选二?② **下掉范围**:正文「表格中 6~24 条下掉」与表内 Todo 列对不上(第 1 行 `lifetime_student` 单独标"下掉"却不在区间;第 25/26 行 `lesson_incomplete_day2_a/b` 同为 0 触发却在区间外),且「6~24」是否含表头行未明。 |
| 2026-07-13 | [语音识别-自适应跟读计时与会话统计](../notes/2026-07-13-语音识别-自适应跟读计时与会话统计.md) | `lesson_speech_recognition_end` 新增字段命名文档内不一致:产品目标段写 `word_session_id`/`speech_word_result`,需求详情表写 `speech_session_id`/`speech_session_result`。以实际埋点上报为准,需向产品/研发确认最终字段名。 |

## 已确认记录

| 日期 | 笔记 | 结论 |
|---|---|---|
| 2026-07-01 | [Quiz 对应多个教学技能标签](../notes/2026-06-30-挑战课与Quiz-Quiz对应多个技能标签.md) | 入库版本确认用 **V1.31.0**;文档「版本号」字段 1.32.0 不作为本库版本。 |
| 2026-07-01 | [Quiz 手动创建题目](../notes/2026-06-30-挑战课与Quiz-Quiz手动创建题目.md) | 入库版本确认用 **V1.31.0**;文档「版本号」字段 1.32.0 不作为本库版本。 |
| 2026-07-01 | [Miniplan 改 Phonics AB 测试](../notes/2026-06-30-Phonics-Miniplan改Phonics%20AB测试.md) | 入库版本确认用 **V1.31.0**;文档 / Figma 1.32.0 不作为本库版本。 |
| 2026-07-01 | [语音识别-降噪增益](../notes/2026-06-30-语音识别-降噪增益.md) | 入库版本确认用 **V1.31.0**;文档「版本号」字段 v1.30.0 不作为本库版本。 |
| 2026-07-01 | [单词短句多语言音频库](../notes/2026-06-30-音频库-单词短句多语言音频库.md) | 入库版本确认用 **V1.31.0**,无排期风险。 |
