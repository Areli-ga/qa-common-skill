# Giggle Academy APP 测试覆盖维度

写用例时逐维过闸。每个维度只能标「已覆盖 / N/A / 待确认」，不能默默跳过。

## 产品链路

- 启动：冷启动、杀进程重启、首装、升级、热更检查、资源下载、失败重试。
- 账号：游客、登录、注册、新老用户、会员/非会员、儿童档案、国家码、权限弹窗。
- 学习：地图、推荐课、进课、课中互动、完课、退出、重进、结算、奖励、进度同步。
- 内容：课程资源、故事书、挑战课、报告、证书、明日预告、搜索、多语言文案。
- 运营：ABTest、后台开关、弹窗、Banner、活动、奖励中心、扭蛋、配置灰度。

## 技术端覆盖

| 端/模块 | 必测关注 |
|---|---|
| Android 原生 | 权限、返回键、生命周期、低端机、安装升级、系统版本、网络切换 |
| iOS 原生 | 权限、前后台切换、弱网恢复、内购/登录态、不同机型安全区 |
| Unity | 资源加载、场景切换、动画/Spine、性能、内存、崩溃、课中状态恢复 |
| H5 | WebView 判定、Bridge 时序、登录态/语言、路由、缓存/离线包、资源 CDN、语音/音频、埋点、旧 WebView 兼容 |
| Flutter | 页面状态、路由栈、原生桥、渲染、键盘/安全区、异步加载 |
| 后端 | 接口入参/出参、错误码、幂等、超时、重试、数据一致性、灰度配置 |

## 通用场景

- 主流程：按 PRD 用户路径从入口到结果完整走通。
- 反向/中断：中途退出、断网、杀进程、切后台、重复点击、快速切换。
- 状态矩阵：新用户/老用户、未解锁/已解锁、未完成/已完成、有奖励/无奖励、有配置/无配置。
- 弱网：无网、2G/3G、超时、资源下载中断、接口失败后恢复。
- 兼容：不同系统、不同屏幕、低端设备、横竖屏或安全区。
- 数据一致性：客户端展示、后端记录、学习进度、奖励数量、埋点和配置命中一致。
- 回归：受影响模块的老路径、相邻入口、历史缺陷路径、提测说明中的关联模块。

## 代码风险库（按需读取）

本文件只保留“测哪些面”的高层覆盖清单。代码扫描得到的端侧细节已拆到独立风险库，**只在命中触发条件时读取对应文件**，不要每次全量灌入用例生成。

| 风险库 | 什么时候读 | 用法 |
|---|---|---|
| `references/code-risk-backend.md` | 涉及接口、奖励、计数、每日/连续、档案隔离、异步生成、多语言兜底 | 从命中的风险点补充后端/数据一致性用例 |
| `references/code-risk-unity.md` | 涉及 Unity 玩法、课程资源、热更、动画、视频、音频、低端机、Unity 与原生通信 | 从命中的风险点补充资源、生命周期、状态机、性能用例 |
| `references/code-risk-native.md` | 涉及 Android/iOS 原生容器、权限、录音、推送、深链、网络、登录态、容器切换 | 从命中的风险点补充端差异和生命周期用例 |
| `references/code-risk-h5.md` | 涉及 H5/WebView、bridge、离线包、manifest、缓存、i18n、音频、神策/Sentry、分享/上传 | 从命中的风险点补充 H5/WebView 专项用例 |

读取规则：
- 先用 PRD 判断端范围；不涉及的端风险库标 N/A，不读取。
- 读取风险库后，只把**命中本需求的触发条件**编入用例；无关代码细节不要硬塞。
- 风险库只提供“容易漏的测试启发”，不能覆盖 PRD；冲突或缺口进入「风险与待确认」。
- 代码风险库不要沉淀密钥、token、账号、内部证书等敏感值。

## 风险提示

- 多端联动需求容易漏测端间状态同步。
- Unity 资源/动画变更容易出现白屏、卡死、闪烁、内存上涨。
- 后台配置/AB 未写清时，必须标 `待确认`，并要求测试前固定配置。
- 热更、资源、启动链路失败时，必须验证可恢复和用户可理解的失败状态。
- PRD 只有视觉稿时，要补问交互、接口、边界、空态、异常和灰度策略。
