interface:
  display_name: "App UI QA"
  short_description: "Case-first app smoke testing via Android adb-only and iPhone Mirroring"
  default_prompt: "Use $app-ui-qa to run or prepare a case-first mobile app smoke test through Android adb-only or iPhone Mirroring plus PyAutoGUI, then produce an HTML report with screenshots."
