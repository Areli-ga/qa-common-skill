interface:
  display_name: "App UI QA"
  short_description: "Case-first Android and iOS smoke testing with visual evidence"
  default_prompt: "Use $app-ui-qa to read the active case in full, run its Android adb-only or iPhone Mirroring smoke path, recover reachable coverage, and produce a screenshot-backed HTML report."
